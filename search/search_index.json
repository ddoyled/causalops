{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"causalops","text":"<p>Registry for causal inference model results on Databricks.</p> <p>Model teams define versioned contracts (ModelSpecs) that declare their result tables and metrics, register them via CLI or CI, and promote them through a lifecycle (experiment \u2192 challenger \u2192 production \u2192 retired). Consumers query results by version or status through a unified API that resolves canonical metric names, joins across tables, and unions across versions automatically.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install causalops\n</code></pre>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>from causalops import RegistryClient\nfrom causalops.store import get_store\nfrom causalops.utils import build_local_spark_session\n\nspark = build_local_spark_session()\nclient = RegistryClient(store=get_store(), spark=spark)\n\n# Query production results\ndf = client.get_results(\n    family=\"uplift\",\n    status=\"production\",\n    metrics=[\"treatment_effect\", \"ci_lower\", \"ci_upper\"],\n)\ndf.show()\n</code></pre>"},{"location":"#documentation","title":"Documentation","text":"<p>This documentation follows the Diataxis framework:</p> Section Purpose Start here if you want to... Tutorials Learning-oriented step-by-step guides ...learn how causalops works from scratch How-To Guides Task-oriented recipes for specific goals ...accomplish a specific task Reference Auto-generated API documentation ...look up a class, function, or CLI flag Explanation Understanding-oriented design discussions ...understand why things work this way"},{"location":"#by-role","title":"By role","text":"<p>Model producer \u2014 you own a causal model and want to register its results.</p> <ul> <li>Define a ModelSpec \u2014 declare your tables and metrics</li> <li>Register &amp; Validate \u2014 validate schemas and register via CLI</li> <li>CI Registration with GitHub Actions \u2014 automate registration on tag push</li> </ul> <p>Platform team \u2014 you manage the model lifecycle across teams.</p> <ul> <li>Promote through Lifecycle \u2014 experiment \u2192 challenger \u2192 production \u2192 retired</li> <li>Custom Store Backend \u2014 implement the SpecStore ABC for your infrastructure</li> <li>Databricks Migration \u2014 lift-and-shift from local dev to Databricks</li> </ul> <p>Consumer \u2014 you query model results from downstream processes.</p> <ul> <li>Query Results \u2014 query by version, status, or point in time</li> <li>Production-of-Record Collection \u2014 build a unified production table across families</li> <li>Champion / Challenger Comparison \u2014 compare two versions side-by-side before cutting over</li> </ul>"},{"location":"explanation/","title":"Explanation","text":"<p>Understanding-oriented articles that explain the why behind design decisions in causalops.</p> <p>These are not step-by-step instructions \u2014 see Tutorials and How-To Guides for that. Instead, these articles give you the reasoning behind the architecture so you can make informed decisions when extending or integrating with the system.</p> <ul> <li>Architecture \u2014 module structure, the three-persona model, and why the package is shaped the way it is.</li> <li>The Lifecycle Model \u2014 why four statuses, atomic production swaps, and append-only event streams.</li> <li>Alias Resolution \u2014 how canonical names and aliases provide cross-version compatibility.</li> <li>Planner Internals \u2014 join strategy, multi-version unions, NULL padding, and the dual-path table reader.</li> </ul>"},{"location":"explanation/alias-resolution/","title":"Alias Resolution","text":""},{"location":"explanation/alias-resolution/#the-problem","title":"The problem","text":"<p>When a model team ships a new version, physical column names in the result tables may change. Version 3.0.0 might store the average treatment effect in a column called <code>ate</code>, while version 3.1.0 renames it to <code>treatment_effect_avg</code> for clarity. If downstream consumers hard-code column names, every schema change breaks their queries.</p>"},{"location":"explanation/alias-resolution/#canonical-names-as-the-stable-api","title":"Canonical names as the stable API","text":"<p>Each <code>Metric</code> in a <code>ModelSpec</code> has two names:</p> <ul> <li><code>name</code> (canonical): the consumer-facing name that stays stable across versions.</li> <li><code>column</code> (physical): the actual column in the result table, which can change.</li> </ul> <pre><code>Metric(name=\"treatment_effect\", column=\"ate\", dtype=\"double\")\n</code></pre> <p>Consumers always request canonical names:</p> <pre><code>client.get_results(family=\"uplift\", metrics=[\"treatment_effect\"])\n</code></pre> <p>The planner resolves <code>\"treatment_effect\"</code> to the physical column <code>\"ate\"</code> through the spec and renames it in the output DataFrame. The consumer never sees <code>\"ate\"</code>.</p>"},{"location":"explanation/alias-resolution/#aliases-for-backwards-compatibility","title":"Aliases for backwards compatibility","text":"<p>When a canonical name is renamed between versions, the old name becomes an alias so existing queries keep working.</p> <p>Suppose v3.0.0 used the canonical name <code>\"te\"</code>:</p> <pre><code># v3.0.0\nMetric(name=\"te\", column=\"ate\", dtype=\"double\")\n</code></pre> <p>In v3.1.0, the team renames it to <code>\"treatment_effect\"</code> but keeps <code>\"te\"</code> as an alias:</p> <pre><code># v3.1.0\nMetric(name=\"treatment_effect\", column=\"ate\", dtype=\"double\", aliases=[\"te\"])\n</code></pre> <p>Now both of these resolve correctly:</p> <pre><code>spec.resolve_metric(\"te\")                # \u2192 (shared_table, \"treatment_effect\", \"ate\")\nspec.resolve_metric(\"treatment_effect\")  # \u2192 (shared_table, \"treatment_effect\", \"ate\")\n</code></pre> <p>A consumer using the old name <code>\"te\"</code> gets the same data they always did. A consumer using the new name <code>\"treatment_effect\"</code> also works. The output column is always named after the canonical name (<code>\"treatment_effect\"</code>), regardless of which lookup name was used.</p>"},{"location":"explanation/alias-resolution/#how-resolve_metric-works","title":"How resolve_metric works","text":"<p><code>ModelSpec.resolve_metric(lookup)</code> walks all tables and all metrics in the spec:</p> <ol> <li>For each metric, check if <code>lookup</code> matches the canonical <code>name</code>.</li> <li>If not, check if <code>lookup</code> appears in the metric's <code>aliases</code> tuple.</li> <li>Return <code>(Table, canonical_name, physical_column)</code> on the first match.</li> <li>Raise <code>KeyError</code> if no metric matches.</li> </ol> <p>The canonical name is checked before aliases, but in practice this ordering doesn't matter because the collision validators (described below) ensure every lookup name is unique across the entire spec.</p>"},{"location":"explanation/alias-resolution/#collision-detection","title":"Collision detection","text":"<p>Ambiguous lookup names would make <code>resolve_metric()</code> non-deterministic, so the spec validators enforce uniqueness at two levels:</p> <p>Within a table: No two metrics can share a canonical name, and no alias can collide with any canonical name or other alias in the same table. This is enforced by <code>Table._no_duplicate_metric_names_or_alias_collisions</code>.</p> <p>Across tables: A lookup name (canonical or alias) that appears in one table cannot appear in another table within the same spec. This is enforced by <code>ModelSpec._no_cross_table_alias_collisions</code>. Without this rule, <code>resolve_metric(\"foo\")</code> could match metrics in two different tables, and the planner wouldn't know which table to read.</p> <p>Additionally, an alias cannot equal its own metric's canonical name \u2014 that would be redundant and signals a likely mistake. This is enforced by <code>Metric._alias_not_equal_to_name</code>.</p>"},{"location":"explanation/alias-resolution/#cross-version-resolution-in-multi-version-queries","title":"Cross-version resolution in multi-version queries","text":"<p>When <code>plan_for_specs</code> unions results across multiple versions, each version's spec independently resolves metrics. This means:</p> <ul> <li>v3.0.0 might resolve <code>\"treatment_effect\"</code> to physical column <code>\"ate\"</code></li> <li>v3.1.0 might resolve <code>\"treatment_effect\"</code> to physical column <code>\"treatment_effect_avg\"</code></li> </ul> <p>Both produce an output column called <code>\"treatment_effect\"</code>, and the union aligns them by name. The <code>version</code> column tags which rows came from which spec, so the physical column difference is invisible to the consumer.</p> <p>If a metric exists in one version but not another, <code>plan_for_specs</code> pads the missing version's rows with NULLs rather than failing. See Planner Internals for details.</p>"},{"location":"explanation/architecture/","title":"Architecture","text":""},{"location":"explanation/architecture/#module-dependency-graph","title":"Module dependency graph","text":"<pre><code>graph TD\n    init[\"causalops.__init__\"] --&gt; client[\"causalops.client\"]\n    init --&gt; spec[\"causalops.spec\"]\n\n    client --&gt; planner[\"causalops.planner\"]\n    client --&gt; store_base[\"causalops.store.base\"]\n\n    planner --&gt; spec\n    planner --&gt; utils[\"causalops.utils\"]\n\n    validation[\"causalops.validation\"] --&gt; spec\n    validation --&gt; utils\n\n    cli[\"causalops.cli\"] --&gt; store_init[\"causalops.store\"]\n    cli --&gt; validation\n    cli -.-&gt;|lazy| utils\n\n    store_init --&gt; paths[\"causalops.paths\"]\n    store_init --&gt; store_base\n    store_init --&gt; json_file[\"causalops.store.json_file\"]\n\n    json_file --&gt; spec\n    json_file --&gt; store_base\n\n    store_base --&gt; spec</code></pre> <p>Solid arrows are import-time dependencies. The dashed arrow from <code>cli</code> to <code>utils</code> is a lazy import \u2014 Spark is only loaded when the <code>register</code> or <code>validate</code> commands actually run.</p>"},{"location":"explanation/architecture/#three-personas","title":"Three personas","text":"<p>The package is designed around three distinct user roles, each touching a different slice of the module graph:</p> <p>Model producer (spec + CLI <code>register</code> / <code>validate</code>) :   The team that owns a causal model. They define a <code>ModelSpec</code> in a <code>model_spec.py</code> file, run <code>causalops validate</code> to check it against the live tables, and <code>causalops register</code> to persist the contract. Their surface is <code>causalops.spec</code> and the CLI.</p> <p>Platform team (store + CLI <code>promote</code>) :   The team that manages the model lifecycle. They promote versions through statuses (<code>experiment</code> \u2192 <code>challenger</code> \u2192 <code>production</code> \u2192 <code>retired</code>) using <code>causalops promote</code>. Their surface is <code>causalops.store</code> and the CLI.</p> <p>Consumer (client + planner) :   Downstream processes that query model results. They use <code>RegistryClient</code> to discover what's registered, then query results by version or status. Their surface is <code>causalops.client</code>, which delegates to the planner internally.</p> <p>This separation means a consumer never needs to know about CLI flags or store internals, and a producer never needs to know how the planner resolves metrics.</p>"},{"location":"explanation/architecture/#why-registryclient-is-a-dataclass","title":"Why RegistryClient is a dataclass","text":"<p>Every consumer query needs two things: a store (to look up registrations and statuses) and a Spark session (to read result tables). Rather than passing both as arguments to every function call, <code>RegistryClient</code> bundles them into a single object:</p> <pre><code>client = RegistryClient(store=get_store(), spark=spark)\nclient.get_results(family=\"uplift\", status=\"production\", metrics=[...])\n</code></pre> <p>This is a deliberate choice over module-level functions. A dataclass (rather than a full class with <code>__init__</code> logic) keeps the object transparent \u2014 you can see exactly what it holds, and testing is trivial because you can inject any <code>SpecStore</code> implementation.</p>"},{"location":"explanation/architecture/#why-store-is-a-subpackage-with-an-abc","title":"Why store is a subpackage with an ABC","text":"<p>The <code>causalops.store</code> subpackage is structured around <code>SpecStore</code>, an abstract base class that defines the storage contract. The concrete <code>JsonFileSpecStore</code> implements it for local development, storing everything in a single JSON file.</p> <p>This abstraction exists for one reason: Databricks portability. On Databricks, the registry would live in a Delta table on Unity Catalog, not a JSON file. By coding all consumer and planner logic against the <code>SpecStore</code> ABC, swapping <code>JsonFileSpecStore</code> for a Delta-backed implementation changes exactly one thing \u2014 the return value of <code>get_store()</code>. Zero consumer code, zero planner code, and zero CLI code needs to change.</p> <p>The <code>get_store()</code> factory reads <code>CAUSALOPS_STORE_CONFIG</code> from the environment to dispatch to the right backend, making the swap a deployment configuration change rather than a code change.</p>"},{"location":"explanation/architecture/#why-the-planner-uses-pyspark","title":"Why the planner uses PySpark","text":"<p>The planner reads result tables and joins them using PySpark DataFrames, not pandas. This is a deliberate portability decision: on Databricks, Spark is the native execution engine. By writing planner logic in PySpark, the same code runs unchanged on a Databricks cluster reading Unity Catalog tables or on a developer's laptop reading local Parquet files.</p> <p>The <code>read_table</code> helper in <code>causalops.utils</code> is the bridge \u2014 it inspects the table path and uses <code>spark.read.parquet()</code> for filesystem paths or <code>spark.table()</code> for catalog identifiers. The planner doesn't know or care which path is taken.</p>"},{"location":"explanation/lifecycle-model/","title":"The Lifecycle Model","text":""},{"location":"explanation/lifecycle-model/#why-four-statuses","title":"Why four statuses","text":"<p>A model version's lifecycle has four states:</p> <pre><code>experiment \u2192 challenger \u2192 production \u2192 retired\n</code></pre> <p>A simpler active/inactive model wouldn't serve the workflow that causal inference teams actually follow. Here's why each status exists:</p> <p>experiment :   The initial state when a version is registered. The model has been validated against the catalog schema and its contract is on record, but nobody has endorsed it for any purpose. It's a staging area.</p> <p>challenger :   The shadow-deployment slot. A challenger version runs alongside the current production version so the team can compare their results side-by-side. Consumers can query both at once:</p> <pre><code>```python\nclient.get_results(\n    family=\"uplift\",\n    status=[\"production\", \"challenger\"],\n    metrics=[\"treatment_effect\"],\n)\n```\n\nThis returns a unioned DataFrame with a `version` column, making A/B comparison straightforward. Without a dedicated challenger status, teams would need ad-hoc version tracking outside the registry.\n</code></pre> <p>production :   The canonical result set that downstream processes depend on. At most one version per family holds this status at any given time (see Atomic production swaps below).</p> <p>retired :   A soft delete with a full audit trail. The version's registration and entire status history remain in the store \u2014 only its active status changes. Retired versions still appear in <code>list_versions()</code> and <code>history()</code>, so you can always reconstruct what happened.</p>"},{"location":"explanation/lifecycle-model/#atomic-production-swaps","title":"Atomic production swaps","text":"<p>When you promote a version to production, the store atomically retires the current production version in the same write. Both status events share the same <code>effective_from</code> timestamp:</p> <pre><code>v1.0.0: PRODUCTION \u2192 RETIRED  (effective_from = T)\nv1.1.0: CHALLENGER \u2192 PRODUCTION (effective_from = T)\n</code></pre> <p>This atomicity prevents a window where two versions simultaneously claim production status. A downstream consumer querying <code>status=\"production\"</code> always gets exactly one version \u2014 never zero, never two.</p> <p>The <code>JsonFileSpecStore</code> achieves this by writing both events to the status log before flushing to disk. Any future store backend (e.g., Delta-backed) must provide the same guarantee, as documented in the <code>SpecStore.promote()</code> contract.</p>"},{"location":"explanation/lifecycle-model/#the-reactivate-guard","title":"The reactivate guard","text":"<p>Promoting a retired version to any other status requires an explicit <code>--reactivate</code> flag:</p> <pre><code>causalops promote --family uplift --version 1.0.0 \\\n    --status challenger --assigned-by \"$USER\" --reactivate\n</code></pre> <p>Without the flag, the promotion is rejected. This friction is intentional: un-retiring a version silently changes what downstream consumers see. If a process queries <code>status=\"challenger\"</code>, a quietly reactivated version would appear in its results without anyone explicitly deciding that should happen.</p> <p>The guard makes reactivation a deliberate act that shows up clearly in the command history and audit log.</p>"},{"location":"explanation/lifecycle-model/#append-only-status-log","title":"Append-only status log","text":"<p>The status log is an append-only event stream, not a mutable status field. Each event records:</p> <ul> <li>What happened (the new status)</li> <li>When it happened (<code>effective_from</code>)</li> <li>Who did it (<code>assigned_by</code>)</li> <li>Why (<code>note</code>)</li> </ul> <p>This design enables point-in-time queries. The <code>as_of</code> parameter on <code>current_status()</code>, <code>by_status()</code>, and <code>get_results()</code> lets you ask \"what was in production on 2026-01-15?\" by replaying the event stream up to that timestamp. A mutable status field would lose this history.</p> <p>The event stream is also preserved across re-registrations. When <code>--force</code> deletes and re-creates a registration row, the status log entries for that (family, version) pair remain. The audit trail survives even when the spec contract changes.</p>"},{"location":"explanation/lifecycle-model/#status-transitions","title":"Status transitions","text":"<p>There are no hard-coded transition rules beyond two constraints:</p> <ol> <li>Production promotion atomically retires the outgoing production version.</li> <li>Un-retiring requires <code>reactivate=True</code>.</li> </ol> <p>Any other transition is allowed \u2014 you can promote directly from <code>experiment</code> to <code>production</code>, or from <code>production</code> back to <code>challenger</code>. The system records what happened rather than enforcing a rigid state machine, because different teams have different rollout workflows.</p>"},{"location":"explanation/planner-internals/","title":"Planner Internals","text":"<p>The planner translates a list of canonical metric names into a PySpark DataFrame by reading physical tables, renaming columns, and joining or unioning as needed. There are two entry points: <code>plan_for_spec</code> (one version) and <code>plan_for_specs</code> (multiple versions).</p>"},{"location":"explanation/planner-internals/#single-spec-planning-plan_for_spec","title":"Single-spec planning: plan_for_spec","text":"<p>Given one <code>ModelSpec</code> and a list of requested metric names, <code>plan_for_spec</code> produces a joined DataFrame:</p> <ol> <li> <p>Resolve metrics. For each requested name, call <code>spec.resolve_metric(name)</code> to get <code>(Table, canonical_name, physical_column)</code>. This groups the requested metrics by the table that owns them.</p> </li> <li> <p>Read and select per table. For each table that has at least one requested metric:</p> <ul> <li>Read the table via <code>read_table(spark, table.path)</code> \u2014 Parquet for filesystem paths, <code>spark.table()</code> for catalog identifiers.</li> <li>Build a select list: the table's key column aliased to the spec's <code>measurement_key</code>, plus each physical column aliased to its canonical name.</li> <li>If <code>include_columns</code> were requested (e.g., <code>run_date</code>, <code>channel_id</code>), attach them from the first table that has them.</li> </ul> </li> <li> <p>Outer-join across tables. If metrics come from multiple tables, the per-table DataFrames are outer-joined on the <code>measurement_key</code>. An outer join ensures rows appear even if one table has entries the other doesn't.</p> </li> <li> <p>Result. One row per <code>measurement_key</code> value, with columns named after canonical metric names.</p> </li> </ol>"},{"location":"explanation/planner-internals/#include_columns","title":"include_columns","text":"<p>The <code>include_columns</code> parameter passes through extra physical columns that aren't declared as metrics \u2014 typically context columns like <code>run_date</code> or <code>channel_id</code> that downstream processes need for filtering.</p> <p>These columns are selected from the first table in the join only, not from every table. This avoids ambiguity \u2014 if multiple tables have a <code>run_date</code> column, including both in a join would create <code>run_date</code> and <code>run_date_1</code> columns. By picking from one table, the output stays clean.</p>"},{"location":"explanation/planner-internals/#multi-spec-planning-plan_for_specs","title":"Multi-spec planning: plan_for_specs","text":"<p>Given multiple <code>ModelSpec</code> objects (typically different versions of the same family), <code>plan_for_specs</code> produces a unioned DataFrame tagged with a <code>version</code> column:</p> <ol> <li> <p>Plan each spec independently. For each spec, determine which of the requested metrics it knows about (via <code>resolve_metric</code>, catching <code>KeyError</code> for unknown ones). Run <code>plan_for_spec</code> on just the known metrics.</p> </li> <li> <p>Tag with version. Add a <code>version</code> column containing <code>spec.version</code> as a literal string.</p> </li> <li> <p>Pad missing metrics with NULL. For any requested metric that a spec doesn't know about, add a NULL column (<code>F.lit(None).cast(\"double\")</code>). This ensures every spec's DataFrame has the same set of columns.</p> </li> <li> <p>Standardize column order. Select columns in a consistent order \u2014 <code>measurement_key</code>, then all requested metrics, then <code>version</code> \u2014 so the union aligns correctly.</p> </li> <li> <p>Union. Combine all per-spec DataFrames with <code>unionByName(allowMissingColumns=True)</code>.</p> </li> </ol>"},{"location":"explanation/planner-internals/#why-null-padding-instead-of-errors","title":"Why NULL padding instead of errors","text":"<p>A newer model version might introduce metrics that older versions don't have. For example, v1.1.0 adds <code>cate_variance</code> while v1.0.0 only has <code>treatment_effect</code>. If a consumer queries both versions for <code>[\"treatment_effect\", \"cate_variance\"]</code>, the planner:</p> <ul> <li>Returns <code>treatment_effect</code> and <code>cate_variance</code> values for v1.1.0 rows</li> <li>Returns <code>treatment_effect</code> values and NULL <code>cate_variance</code> for v1.0.0 rows</li> </ul> <p>This is the right behavior for cross-version comparison. The alternative \u2014 raising an error when any version is missing a requested metric \u2014 would make it impossible to compare versions with different metric sets, which is exactly when comparison is most valuable (evaluating whether a new metric is worth adding).</p>"},{"location":"explanation/planner-internals/#the-read_table-dual-path","title":"The read_table dual-path","text":"<p><code>read_table(spark, path)</code> in <code>causalops.utils</code> inspects the path string to decide how to read:</p> <ul> <li>If the path contains <code>/</code> or <code>://</code> \u2192 filesystem path \u2192 <code>spark.read.parquet(path)</code></li> <li>Otherwise \u2192 catalog identifier \u2192 <code>spark.table(path)</code> (Unity Catalog on Databricks, Hive metastore elsewhere)</li> </ul> <p>This is the Databricks portability seam at the data layer. A <code>ModelSpec</code> uses filesystem paths in local development:</p> <pre><code>Table(path=\".causalops/data/uplift/shared_v3.parquet\", ...)\n</code></pre> <p>And Unity Catalog identifiers on Databricks:</p> <pre><code>Table(path=\"main.uplift.shared_v3\", ...)\n</code></pre> <p>The planner code is identical in both cases \u2014 only the <code>path</code> values in the spec change. This is typically handled by an environment-aware helper in the spec module (see Databricks Migration).</p>"},{"location":"how-to/","title":"How-To Guides","text":"<p>Task-oriented recipes for common causalops workflows. Each guide assumes you're familiar with the basics covered in the Tutorials.</p> Guide When to use it Production-of-Record Collection Build a unified table of production results across model families and version windows Champion / Challenger Comparison Compare champion and challenger metrics side-by-side before cutting over CI Registration with GitHub Actions Automate spec registration on every semver tag push Custom Store Backend Implement the <code>SpecStore</code> ABC for a new storage system Databricks Migration Lift-and-shift from local dev to Databricks Unity Catalog"},{"location":"how-to/ci-registration/","title":"Automate Registration with GitHub Actions","text":"<p>Register a <code>ModelSpec</code> automatically every time a semver tag is pushed.</p>"},{"location":"how-to/ci-registration/#how-it-works","title":"How it works","text":"<p>The <code>causalops register</code> CLI reads git metadata from environment variables that GitHub Actions sets automatically:</p> CLI flag Environment variable Set by GHA <code>--git-repo</code> <code>GITHUB_REPOSITORY</code> Yes <code>--git-tag</code> <code>GITHUB_REF_NAME</code> Yes <code>--git-sha</code> <code>GITHUB_SHA</code> Yes <code>--registered-by</code> <code>GITHUB_ACTOR</code> Yes <p>This means the <code>register</code> command needs no explicit flags in a GHA context.</p>"},{"location":"how-to/ci-registration/#workflow-file","title":"Workflow file","text":"<p>Create <code>.github/workflows/register.yml</code>:</p> <pre><code>name: Register model spec\non:\n  push:\n    tags: ['v*.*.*']\n\njobs:\n  register:\n    runs-on: ubuntu-latest\n    permissions:\n      id-token: write\n      contents: read\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: '3.12'\n      - name: Install SDK\n        run: pip install causalops\n      - name: Type-check the spec\n        run: |\n          pip install mypy\n          mypy model_spec.py --strict\n      - name: Register\n        env:\n          CAUSALOPS_STORE_CONFIG: ${{ vars.CAUSALOPS_STORE_CONFIG }}\n        run: causalops register --spec-path model_spec.py\n</code></pre>"},{"location":"how-to/ci-registration/#step-by-step-breakdown","title":"Step-by-step breakdown","text":""},{"location":"how-to/ci-registration/#1-trigger-on-semver-tags","title":"1. Trigger on semver tags","text":"<pre><code>on:\n  push:\n    tags: ['v*.*.*']\n</code></pre> <p>The workflow fires only when you push a tag matching the semver pattern (e.g., <code>v3.1.0</code>). The CLI validates that the tag matches the <code>version</code> field in the spec \u2014 <code>v3.1.0</code> must correspond to <code>version=\"3.1.0\"</code> in your <code>ModelSpec</code>.</p>"},{"location":"how-to/ci-registration/#2-type-check-the-spec","title":"2. Type-check the spec","text":"<pre><code>- name: Type-check the spec\n  run: |\n    pip install mypy\n    mypy model_spec.py --strict\n</code></pre> <p>Running <code>mypy --strict</code> catches type errors in the spec definition before registration. This is optional but recommended \u2014 it verifies that <code>Metric</code>, <code>Table</code>, and <code>ModelSpec</code> types are used correctly.</p>"},{"location":"how-to/ci-registration/#3-configure-the-store-backend","title":"3. Configure the store backend","text":"<pre><code>env:\n  CAUSALOPS_STORE_CONFIG: ${{ vars.CAUSALOPS_STORE_CONFIG }}\n</code></pre> <p>Set <code>CAUSALOPS_STORE_CONFIG</code> as a repository variable in GitHub Settings. For a Databricks-backed registry:</p> <pre><code>{\"backend\": \"delta\", \"database\": \"main.registry\"}\n</code></pre> <p>For the default JSON file store (local dev or testing), omit the variable entirely.</p>"},{"location":"how-to/ci-registration/#4-permissions-for-databricks-auth","title":"4. Permissions for Databricks auth","text":"<pre><code>permissions:\n  id-token: write\n  contents: read\n</code></pre> <p>The <code>id-token: write</code> permission is needed if your registration step authenticates to Databricks using OIDC (workload identity federation). If you use a service principal token instead, you only need <code>contents: read</code>.</p> <p>Note</p> <p>The register step also runs <code>validate_against_uc</code>, which reads the declared tables to check column names and dtypes. Make sure the runner can reach the data catalog (Databricks workspace or local Parquet files).</p>"},{"location":"how-to/ci-registration/#tagging-workflow","title":"Tagging workflow","text":"<pre><code># In your model repo, after updating model_spec.py:\ngit tag v3.2.0\ngit push origin v3.2.0\n</code></pre> <p>The workflow picks up the tag, validates the spec, and registers it to the configured store.</p>"},{"location":"how-to/custom-store-backend/","title":"Implement a Custom Store Backend","text":"<p>Create a new storage backend by implementing the <code>SpecStore</code> abstract base class.</p>"},{"location":"how-to/custom-store-backend/#the-specstore-contract","title":"The SpecStore contract","text":"<p><code>SpecStore</code> defines 9 abstract methods organized into two conceptual tables:</p> <ul> <li>Registrations \u2014 immutable, one row per <code>(family, version)</code>.</li> <li>Status log \u2014 append-only status events per <code>(family, version)</code>.</li> </ul> <pre><code>from causalops.store.base import SpecStore\n</code></pre>"},{"location":"how-to/custom-store-backend/#registration-methods","title":"Registration methods","text":"Method Contract <code>put(spec, *, git_repo, git_tag, git_sha, registered_by, sdk_version, registered_at=None)</code> Insert a registration and its initial <code>EXPERIMENT</code> status event. Raise <code>KeyError</code> if <code>(family, version)</code> already exists. <code>registered_at</code> defaults to <code>datetime.now(UTC)</code>. <code>exists(family, version)</code> Return <code>True</code> if <code>(family, version)</code> is registered. <code>get(family, version)</code> Return the <code>Registration</code> record. Raise <code>KeyError</code> if not found. <code>list_families()</code> Return all registered family names. <code>list_versions(family)</code> Return all registered versions for a family."},{"location":"how-to/custom-store-backend/#status-methods","title":"Status methods","text":"Method Contract <code>promote(family, version, status, *, assigned_by, note=\"\", reactivate=False, effective_from=None)</code> Append a status event. See atomicity requirements below. <code>current_status(family, version, *, as_of=None)</code> Return the current status. When <code>as_of</code> is set, return the status at that point in time. <code>by_status(family, status, *, as_of=None)</code> Return all registrations matching the given status(es). <code>status</code> can be a single <code>Status</code> or a list. <code>history(family, version)</code> Return all status events, ordered by <code>effective_from</code>."},{"location":"how-to/custom-store-backend/#the-promote-atomicity-invariant","title":"The promote() atomicity invariant","text":"<p>The most critical contract is in <code>promote()</code>:</p> <p>Atomic production swap</p> <p>When promoting a version to <code>PRODUCTION</code>, the implementation must atomically retire the current production version. Both the incoming version's <code>PRODUCTION</code> event and the outgoing version's <code>RETIRED</code> event must share the same <code>effective_from</code> timestamp and be committed in a single write.</p> <p>This prevents a window where two versions simultaneously claim production status.</p> <p>Additional <code>promote()</code> rules:</p> <ul> <li>Reactivate guard: Promoting a <code>RETIRED</code> version to any status requires <code>reactivate=True</code>. Without it, raise <code>ValueError</code>. This is a safety mechanism \u2014 un-retiring silently changes what downstream consumers see.</li> <li><code>effective_from</code>: Defaults to <code>datetime.now(UTC)</code>. Override it for deterministic seed scripts or tests.</li> </ul>"},{"location":"how-to/custom-store-backend/#skeleton-implementation","title":"Skeleton implementation","text":"<pre><code>from datetime import datetime, UTC\nfrom causalops.spec import ModelSpec\nfrom causalops.store.base import (\n    Registration,\n    SpecStore,\n    Status,\n    StatusEvent,\n)\n\n\nclass DeltaSpecStore(SpecStore):\n    \"\"\"SpecStore backed by Delta tables on Unity Catalog.\"\"\"\n\n    def __init__(self, database: str, spark) -&gt; None:\n        self.database = database\n        self.spark = spark\n\n    def put(\n        self,\n        spec: ModelSpec,\n        *,\n        git_repo: str,\n        git_tag: str,\n        git_sha: str,\n        registered_by: str,\n        sdk_version: str,\n        registered_at: datetime | None = None,\n    ) -&gt; Registration:\n        at = registered_at or datetime.now(UTC)\n        if self.exists(spec.family, spec.version):\n            raise KeyError(f\"{spec.family}@{spec.version} already registered\")\n\n        # Write to registrations table + initial EXPERIMENT event\n        # in a single transaction.\n        ...\n\n        return Registration(\n            spec=spec,\n            git_repo=git_repo,\n            git_tag=git_tag,\n            git_sha=git_sha,\n            sdk_version=sdk_version,\n            registered_by=registered_by,\n            registered_at=at,\n        )\n\n    def exists(self, family: str, version: str) -&gt; bool: ...\n    def get(self, family: str, version: str) -&gt; Registration: ...\n    def list_families(self) -&gt; list[str]: ...\n    def list_versions(self, family: str) -&gt; list[str]: ...\n\n    def promote(\n        self,\n        family: str,\n        version: str,\n        status: Status,\n        *,\n        assigned_by: str,\n        note: str = \"\",\n        reactivate: bool = False,\n        effective_from: datetime | None = None,\n    ) -&gt; None:\n        at = effective_from or datetime.now(UTC)\n        current = self.current_status(family, version)\n\n        if current == Status.RETIRED and not reactivate:\n            raise ValueError(\n                f\"{family}@{version} is retired; pass reactivate=True to un-retire\"\n            )\n\n        if status == Status.PRODUCTION:\n            # Atomically: write PRODUCTION event for this version\n            # AND RETIRED event for the current production version (if any).\n            # Both events must share the same effective_from timestamp.\n            ...\n        else:\n            # Write a single status event.\n            ...\n\n    def current_status(self, family: str, version: str, *, as_of: datetime | None = None) -&gt; Status: ...\n    def by_status(self, family: str, status: Status | list[Status], *, as_of: datetime | None = None) -&gt; list[Registration]: ...\n    def history(self, family: str, version: str) -&gt; list[StatusEvent]: ...\n</code></pre> <p>Tip</p> <p>Use <code>JsonFileSpecStore</code> as your reference implementation. It demonstrates all the edge cases: atomic production swaps, the reactivate guard, and point-in-time <code>as_of</code> queries.</p>"},{"location":"how-to/custom-store-backend/#wiring-into-the-factory","title":"Wiring into the factory","text":"<p>The <code>get_store()</code> factory reads the <code>CAUSALOPS_STORE_CONFIG</code> environment variable (JSON) and dispatches on the <code>\"backend\"</code> key.</p> <p>To register your backend, add an <code>elif</code> branch in <code>causalops/store/__init__.py</code>:</p> <pre><code>def get_store() -&gt; SpecStore:\n    raw = os.environ.get(\"CAUSALOPS_STORE_CONFIG\")\n    if raw:\n        cfg = json.loads(raw)\n    else:\n        cfg = {\"backend\": \"json_file\", \"path\": str(default_registry_json_path())}\n\n    backend = cfg.get(\"backend\", \"json_file\")\n    if backend == \"json_file\":\n        return JsonFileSpecStore(path=cfg[\"path\"])\n    elif backend == \"delta\":\n        from your_module import DeltaSpecStore\n        from pyspark.sql import SparkSession\n        spark = SparkSession.getActiveSession()\n        return DeltaSpecStore(database=cfg[\"database\"], spark=spark)\n    raise ValueError(f\"unsupported backend {backend!r}\")\n</code></pre> <p>Then configure it:</p> <pre><code>export CAUSALOPS_STORE_CONFIG='{\"backend\": \"delta\", \"database\": \"main.registry\"}'\n</code></pre>"},{"location":"how-to/databricks-migration/","title":"Migrate to Databricks","text":"<p>The causalops SDK is designed for lift-and-shift to Databricks. The planner, validator, and <code>RegistryClient</code> code run unchanged \u2014 only two configuration seams move.</p>"},{"location":"how-to/databricks-migration/#what-stays-the-same","title":"What stays the same","text":"<p>Everything a consumer touches works identically on Databricks:</p> <ul> <li><code>RegistryClient.get_results()</code> \u2014 same API, same DataFrame output</li> <li><code>plan_for_spec()</code> / <code>plan_for_specs()</code> \u2014 same join and union logic</li> <li><code>validate_against_uc()</code> \u2014 same schema checks</li> <li><code>causalops register</code> / <code>causalops promote</code> \u2014 same CLI commands</li> </ul>"},{"location":"how-to/databricks-migration/#what-changes","title":"What changes","text":""},{"location":"how-to/databricks-migration/#1-registry-backend","title":"1. Registry backend","text":"<p>Swap the JSON file store for a Delta-backed store by setting the <code>CAUSALOPS_STORE_CONFIG</code> environment variable:</p> Local dev (default)Databricks <pre><code># No config needed \u2014 defaults to .causalops/registry.json\n</code></pre> <pre><code>export CAUSALOPS_STORE_CONFIG='{\"backend\": \"delta\", \"database\": \"main.registry\"}'\n</code></pre> <p>The Delta-backed store implements the same <code>SpecStore</code> ABC, so all consumer and CLI code works without changes. See Custom Store Backend for implementation details.</p>"},{"location":"how-to/databricks-migration/#2-result-table-paths","title":"2. Result table paths","text":"<p><code>Table.path</code> values in your <code>model_spec.py</code> switch from filesystem paths to Unity Catalog identifiers:</p> Local devDatabricks <pre><code>Table(\n    name=\"shared\",\n    path=\".causalops/data/uplift/shared_v3.parquet\",\n    key=\"experiment_id\",\n    metrics=(...),\n)\n</code></pre> <pre><code>Table(\n    name=\"shared\",\n    path=\"main.uplift.shared_v3\",\n    key=\"experiment_id\",\n    metrics=(...),\n)\n</code></pre> <p>The <code>read_table</code> helper in <code>causalops.utils</code> handles this transparently: if the path contains <code>/</code> or <code>://</code>, it reads via <code>spark.read.parquet()</code>; otherwise it reads via <code>spark.table()</code>. No planner code changes.</p> <p>Environment-aware paths</p> <p>Keep <code>Table.path</code> values in a small helper inside your spec module that switches on environment:</p> <pre><code>import os\nfrom causalops.paths import default_data_dir\n\ndef table_path(table_name: str, version: str) -&gt; str:\n    if os.environ.get(\"DATABRICKS_RUNTIME_VERSION\"):\n        return f\"main.uplift.{table_name}_v{version}\"\n    return str(default_data_dir() / \"uplift\" / f\"{table_name}_v{version}.parquet\")\n</code></pre> <p>This keeps the <code>ModelSpec</code> definition identical between environments \u2014 only the helper's return value changes.</p>"},{"location":"how-to/databricks-migration/#3-spark-session","title":"3. Spark session","text":"<p>On Databricks, use the cluster-provided session instead of <code>build_local_spark_session()</code>:</p> Local devDatabricks notebook <pre><code>from causalops.utils import build_local_spark_session\n\nspark = build_local_spark_session()\nclient = RegistryClient(store=get_store(), spark=spark)\n</code></pre> <pre><code># `spark` is already available in the notebook context\nclient = RegistryClient(store=get_store(), spark=spark)\n</code></pre> <p>The <code>build_local_spark_session()</code> factory is only used for local dev \u2014 it creates a minimal Spark session with no Hive metastore. On a Databricks cluster, the pre-configured session already has access to Unity Catalog.</p>"},{"location":"how-to/databricks-migration/#migration-checklist","title":"Migration checklist","text":"<ul> <li>[ ] Implement or deploy a Delta-backed <code>SpecStore</code> (see Custom Store Backend)</li> <li>[ ] Set <code>CAUSALOPS_STORE_CONFIG</code> in your Databricks workspace environment</li> <li>[ ] Update <code>Table.path</code> values in <code>model_spec.py</code> to use UC identifiers (or use an env-aware helper)</li> <li>[ ] Remove <code>build_local_spark_session()</code> calls \u2014 use the cluster session</li> <li>[ ] Run <code>causalops validate --spec-path model_spec.py</code> to verify the spec resolves against live UC tables</li> <li>[ ] Update CI workflow to authenticate to Databricks and set <code>CAUSALOPS_STORE_CONFIG</code></li> </ul>"},{"location":"how-to/multi-version-comparison/","title":"Champion / Challenger Comparison","text":"<p>Query champion and challenger results side-by-side to evaluate a new model version before cutting over.</p>"},{"location":"how-to/multi-version-comparison/#pulling-two-versions-by-version-string","title":"Pulling two versions by version string","text":"<p>Pass a list of version strings to <code>get_results()</code> to union results across the champion and challenger:</p> <pre><code>from causalops import RegistryClient\nfrom causalops.store import get_store\nfrom causalops.utils import build_local_spark_session\n\nspark = build_local_spark_session()\nclient = RegistryClient(store=get_store(), spark=spark)\n\nCHAMPION = \"1.0.0\"\nCHALLENGER = \"1.1.0\"\n\ndf = client.get_results(\n    family=\"uplift\",\n    versions=[CHAMPION, CHALLENGER],\n    metrics=[\"treatment_effect\", \"ci_lower\", \"ci_upper\"],\n)\ndf.show()\n</code></pre> <p>The returned DataFrame includes a <code>version</code> column tagging which version each row came from:</p> <pre><code>+--------------+----------------+----------+----------+-------+\n|experiment_id |treatment_effect|ci_lower  |ci_upper  |version|\n+--------------+----------------+----------+----------+-------+\n|exp_001       |0.042           |0.028     |0.056     |1.0.0  |\n|exp_001       |0.039           |0.025     |0.053     |1.1.0  |\n|...           |...             |...       |...       |...    |\n+--------------+----------------+----------+----------+-------+\n</code></pre> <p>You can also query by status when you don't know the exact version strings:</p> <pre><code>df = client.get_results(\n    family=\"uplift\",\n    status=[\"production\", \"challenger\"],\n    metrics=[\"treatment_effect\", \"ci_lower\", \"ci_upper\"],\n)\n</code></pre> <p>Missing metrics</p> <p>When the challenger introduces metrics that the champion doesn't have, the missing columns are filled with <code>NULL</code> rather than raising an error:</p> <pre><code>df = client.get_results(\n    family=\"uplift\",\n    versions=[CHAMPION, CHALLENGER],\n    metrics=[\"treatment_effect\", \"cate_variance\"],\n)\n</code></pre> <p>If <code>cate_variance</code> exists only in v1.1.0 (the challenger), the champion rows will have <code>NULL</code> in that column. This is handled by <code>plan_for_specs</code> internally.</p>"},{"location":"how-to/multi-version-comparison/#side-by-side-pivot","title":"Side-by-side pivot","text":"<p>To reshape the union into a side-by-side comparison, pivot on the <code>version</code> column:</p> <pre><code>from pyspark.sql import functions as F\n\ncomparison = (\n    df.groupBy(\"experiment_id\")\n    .pivot(\"version\")\n    .agg(\n        F.first(\"treatment_effect\").alias(\"treatment_effect\"),\n        F.first(\"ci_lower\").alias(\"ci_lower\"),\n        F.first(\"ci_upper\").alias(\"ci_upper\"),\n    )\n)\ncomparison.show()\n</code></pre> <p>This produces one row per experiment with columns like <code>1.0.0_treatment_effect</code> and <code>1.1.0_treatment_effect</code>.</p>"},{"location":"how-to/multi-version-comparison/#computing-deltas","title":"Computing deltas","text":"<pre><code>delta = comparison.withColumn(\n    \"te_delta\",\n    F.col(f\"`{CHALLENGER}_treatment_effect`\") - F.col(f\"`{CHAMPION}_treatment_effect`\"),\n).select(\n    \"experiment_id\",\n    F.col(f\"`{CHAMPION}_treatment_effect`\").alias(\"champion_te\"),\n    F.col(f\"`{CHALLENGER}_treatment_effect`\").alias(\"challenger_te\"),\n    \"te_delta\",\n)\ndelta.show()\n</code></pre> <p>Tip</p> <p>Use <code>as_of</code> to compare what was in production at a specific point in time:</p> <pre><code>df = client.get_results(\n    family=\"uplift\",\n    status=\"production\",\n    metrics=[\"treatment_effect\"],\n    as_of=\"2026-08-01\",\n)\n</code></pre> <p>Full example</p> <p>See <code>examples/champion_challenger.py</code> for a runnable script that pulls both versions, pivots, computes deltas, and prints summary statistics.</p>"},{"location":"how-to/production-of-record/","title":"Build Production Results Table for Downstream Reporting","text":"<p>Collect each model family's production results into a single DataFrame, respecting the date windows during which each version held production status.</p>"},{"location":"how-to/production-of-record/#the-pattern","title":"The pattern","text":"<p>For each model family:</p> <ol> <li>Call <code>client.production_windows(family)</code> to get <code>(version, start_date, end_date)</code> tuples.</li> <li>For each window, use <code>plan_for_spec()</code> to pull that version's results within its date range.</li> <li>Tag rows with <code>family</code> and <code>version</code>, then union across families.</li> </ol>"},{"location":"how-to/production-of-record/#setup","title":"Setup","text":"<pre><code>from causalops import RegistryClient\nfrom causalops.store import get_store\nfrom causalops.utils import build_local_spark_session\n\nspark = build_local_spark_session()\nclient = RegistryClient(store=get_store(), spark=spark)\n</code></pre>"},{"location":"how-to/production-of-record/#collecting-results-for-one-family","title":"Collecting results for one family","text":"<pre><code>from functools import reduce\n\nfrom pyspark.sql import DataFrame\nfrom pyspark.sql import functions as F\n\nfrom causalops.planner import plan_for_spec\n\n\ndef collect_production_results(\n    client: RegistryClient,\n    family: str,\n    metrics: list[str],\n) -&gt; DataFrame:\n    \"\"\"Union each production window's rows, aliased to canonical metric names.\"\"\"\n    parts: list[DataFrame] = []\n    for version, start, end in client.production_windows(family):\n        spec = client.describe(family, version).spec\n        df = (\n            plan_for_spec(\n                client.spark,\n                spec,\n                metrics=metrics,\n                include_columns=[\"run_date\", \"channel_id\"],\n            )\n            .filter(F.col(\"run_date\") &gt;= start)\n            .withColumn(\"family\", F.lit(family))\n            .withColumn(\"version\", F.lit(version))\n        )\n        if end is not None:\n            df = df.filter(F.col(\"run_date\") &lt; end)\n        parts.append(df)\n    return reduce(lambda a, b: a.unionByName(b, allowMissingColumns=True), parts)\n</code></pre> <p>Key details:</p> <ul> <li><code>include_columns=[\"run_date\", \"channel_id\"]</code> passes through physical columns from the first table so you can filter and partition by them.</li> <li>Open windows (<code>end is None</code>) mean the version is still in production.</li> <li><code>unionByName(allowMissingColumns=True)</code> handles cases where different versions expose different metrics.</li> </ul>"},{"location":"how-to/production-of-record/#combining-across-families","title":"Combining across families","text":"<pre><code>OVERLAPPING_METRICS = [\n    \"target_total\",\n    \"target_incremental\",\n    \"target_incremental_pct\",\n    \"target_ci_hi\",\n    \"target_ci_lo\",\n]\n\nFAMILIES = [\"scm\", \"bsts\"]\n\nparts = [collect_production_results(client, f, OVERLAPPING_METRICS) for f in FAMILIES]\ncombined = reduce(lambda a, b: a.unionByName(b, allowMissingColumns=True), parts)\n\ncombined.orderBy(\"family\", \"channel_id\", \"run_date\").show(truncate=False)\n</code></pre> <p>Tip</p> <p>Choose <code>OVERLAPPING_METRICS</code> to include only the canonical names shared across families. Family-specific metrics (like <code>seasonality_impact_mean</code> in BSTS) can be queried separately per family.</p>"},{"location":"how-to/production-of-record/#running-the-full-example","title":"Running the full example","text":"<p>The repository includes a complete working version of this pattern:</p> <pre><code>python scripts/seed_examples.py\npython examples/keystone.py\n</code></pre> <p>See <code>examples/keystone.py</code> for the source.</p>"},{"location":"reference/","title":"API Reference","text":"<p>The <code>causalops</code> package is organized around three personas:</p> Persona Primary modules Typical tasks Producer (model team) Spec Models, Validation, CLI Define specs, validate schemas, register versions Consumer (downstream analytics) RegistryClient, Planner Query results, discover versions, build production tables for downstream reporting Platform team Store, CLI Promote versions, implement storage backends <p>Supporting modules:</p> <ul> <li>Utilities \u2014 Spark session factory and table reader</li> <li>Paths \u2014 Default filesystem locations for local dev</li> </ul>"},{"location":"reference/cli/","title":"CLI Reference","text":"<p>The <code>causalops</code> command-line interface provides three commands for registering specs, validating schemas, and promoting versions through the lifecycle.</p> <pre><code>causalops [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> Option Description <code>--version</code> Show the version and exit. <code>--help</code> Show this message and exit."},{"location":"reference/cli/#register","title":"<code>register</code>","text":"<p>Register a ModelSpec from a local <code>model_spec.py</code>.</p> <pre><code>causalops register [OPTIONS]\n</code></pre> <p>Options:</p> Option Default Env var Description <code>--spec-path PATH</code> <code>model_spec.py</code> Path to a Python file defining a <code>spec</code> variable of type <code>ModelSpec</code>. <code>--git-repo TEXT</code> (required) <code>GITHUB_REPOSITORY</code> Repository identifier (e.g. <code>org/repo</code>). <code>--git-tag TEXT</code> (required) <code>GITHUB_REF_NAME</code> Git tag \u2014 must match <code>v{spec.version}</code>. <code>--git-sha TEXT</code> (required) <code>GITHUB_SHA</code> Full commit SHA. <code>--registered-by TEXT</code> (required) <code>GITHUB_ACTOR</code> Who is registering this spec. <code>--force</code> <code>false</code> Overwrite an existing registration for this (family, version). <p>The command loads the spec from <code>--spec-path</code>, validates that <code>--git-tag</code> matches <code>v{spec.version}</code>, runs schema validation against the live tables, and persists the registration with an initial <code>experiment</code> status.</p>"},{"location":"reference/cli/#validate","title":"<code>validate</code>","text":"<p>Dry-run schema check for a ModelSpec (no registration).</p> <pre><code>causalops validate [OPTIONS]\n</code></pre> <p>Options:</p> Option Default Description <code>--spec-path PATH</code> <code>model_spec.py</code> Path to a Python file defining a <code>spec</code> variable. <p>Warnings (missing tables) print to stderr and exit 0. Errors (missing columns, dtype mismatches) print to stderr and exit non-zero.</p>"},{"location":"reference/cli/#promote","title":"<code>promote</code>","text":"<p>Append a status event for (family, version).</p> <pre><code>causalops promote [OPTIONS]\n</code></pre> <p>Options:</p> Option Default Env var Description <code>--family TEXT</code> (required) Model family name. <code>--version TEXT</code> (required) Semver version string. <code>--status [experiment\\|challenger\\|production\\|retired]</code> (required) Target status. <code>--assigned-by TEXT</code> (required) <code>USER</code> Who is performing the promotion. <code>--note TEXT</code> <code>\"\"</code> Optional note for the status event. <code>--reactivate</code> <code>false</code> Required when un-retiring a version (safety guard). <p>Promoting to <code>production</code> atomically retires the current production version. Un-retiring a <code>retired</code> version requires the <code>--reactivate</code> flag.</p>"},{"location":"reference/client/","title":"RegistryClient","text":"<p>The consumer-facing entry point for querying the registry. Bundles a store and a Spark session, then exposes discovery, lifecycle, and query methods.</p>"},{"location":"reference/client/#causalops.client.RegistryClient","title":"RegistryClient  <code>dataclass</code>","text":"<pre><code>RegistryClient(store: SpecStore, spark: SparkSession)\n</code></pre>"},{"location":"reference/client/#causalops.client.RegistryClient.store","title":"store  <code>instance-attribute</code>","text":"<pre><code>store: SpecStore\n</code></pre>"},{"location":"reference/client/#causalops.client.RegistryClient.spark","title":"spark  <code>instance-attribute</code>","text":"<pre><code>spark: SparkSession\n</code></pre>"},{"location":"reference/client/#causalops.client.RegistryClient.list_families","title":"list_families","text":"<pre><code>list_families() -&gt; list[str]\n</code></pre> <p>Return all registered family names.</p> Source code in <code>src/causalops/client.py</code> <pre><code>def list_families(self) -&gt; list[str]:\n    \"\"\"Return all registered family names.\"\"\"\n    return self.store.list_families()\n</code></pre>"},{"location":"reference/client/#causalops.client.RegistryClient.list_versions","title":"list_versions","text":"<pre><code>list_versions(family: str) -&gt; list[str]\n</code></pre> <p>Return all registered versions for <code>family</code>.</p> Source code in <code>src/causalops/client.py</code> <pre><code>def list_versions(self, family: str) -&gt; list[str]:\n    \"\"\"Return all registered versions for ``family``.\"\"\"\n    return self.store.list_versions(family)\n</code></pre>"},{"location":"reference/client/#causalops.client.RegistryClient.describe","title":"describe","text":"<pre><code>describe(family: str, version: str) -&gt; Registration\n</code></pre> <p>Return the full registration record for one (family, version).</p> Source code in <code>src/causalops/client.py</code> <pre><code>def describe(self, family: str, version: str) -&gt; Registration:\n    \"\"\"Return the full registration record for one (family, version).\"\"\"\n    return self.store.get(family, version)\n</code></pre>"},{"location":"reference/client/#causalops.client.RegistryClient.history","title":"history","text":"<pre><code>history(family: str, version: str) -&gt; list[StatusEvent]\n</code></pre> <p>Return the status-event log for one (family, version).</p> Source code in <code>src/causalops/client.py</code> <pre><code>def history(self, family: str, version: str) -&gt; list[StatusEvent]:\n    \"\"\"Return the status-event log for one (family, version).\"\"\"\n    return self.store.history(family, version)\n</code></pre>"},{"location":"reference/client/#causalops.client.RegistryClient.production_windows","title":"production_windows","text":"<pre><code>production_windows(\n    family: str,\n) -&gt; list[tuple[str, str, str | None]]\n</code></pre> <p>Return <code>[(version, start_iso, end_iso_or_None), ...]</code> for each production stint.</p> <p>A version's production window opens on its <code>PRODUCTION</code> event and closes on the subsequent <code>RETIRED</code> event (or stays open if never retired).</p> Source code in <code>src/causalops/client.py</code> <pre><code>def production_windows(self, family: str) -&gt; list[tuple[str, str, str | None]]:\n    \"\"\"Return ``[(version, start_iso, end_iso_or_None), ...]`` for each production stint.\n\n    A version's production window opens on its ``PRODUCTION`` event and\n    closes on the subsequent ``RETIRED`` event (or stays open if never\n    retired).\n    \"\"\"\n    windows: list[tuple[str, str, str | None]] = []\n    for version in self.store.list_versions(family):\n        events = sorted(self.store.history(family, version), key=lambda e: e.effective_from)\n        prod_start = None\n        for e in events:\n            if e.status == Status.PRODUCTION and prod_start is None:\n                prod_start = e.effective_from\n            elif e.status == Status.RETIRED and prod_start is not None:\n                windows.append(\n                    (\n                        version,\n                        prod_start.date().isoformat(),\n                        e.effective_from.date().isoformat(),\n                    )\n                )\n                prod_start = None\n        if prod_start is not None:\n            windows.append((version, prod_start.date().isoformat(), None))\n    return windows\n</code></pre>"},{"location":"reference/client/#causalops.client.RegistryClient.get_results","title":"get_results","text":"<pre><code>get_results(\n    *,\n    family: str,\n    metrics: Iterable[str],\n    version: str | None = None,\n    versions: list[str] | None = None,\n    status: str | list[str] | None = None,\n    as_of: datetime | str | None = None,\n) -&gt; DataFrame\n</code></pre> <p>Query metric results for a family, resolved to canonical column names.</p> <p>Select versions by explicit <code>version</code>, a list of <code>versions</code>, or by <code>status</code> (mutually exclusive). When multiple versions match, rows are unioned with a <code>version</code> tag. <code>as_of</code> narrows status lookups to a point in time.</p> Source code in <code>src/causalops/client.py</code> <pre><code>def get_results(\n    self,\n    *,\n    family: str,\n    metrics: Iterable[str],\n    version: str | None = None,\n    versions: list[str] | None = None,\n    status: str | list[str] | None = None,\n    as_of: datetime | str | None = None,\n) -&gt; DataFrame:\n    \"\"\"Query metric results for a family, resolved to canonical column names.\n\n    Select versions by explicit ``version``, a list of ``versions``, or by\n    ``status`` (mutually exclusive). When multiple versions match, rows are\n    unioned with a ``version`` tag.\n    ``as_of`` narrows status lookups to a point in time.\n    \"\"\"\n    selectors = sum(x is not None for x in (version, versions, status))\n    if selectors &gt; 1:\n        raise ValueError(\"pass exactly one of `version`, `versions`, or `status`\")\n    if selectors == 0:\n        raise ValueError(\"one of `version`, `versions`, or `status` is required\")\n\n    if as_of is not None and isinstance(as_of, str):\n        as_of = datetime.fromisoformat(as_of)\n\n    if version is not None:\n        regs = [self.store.get(family, version)]\n    elif versions is not None:\n        regs = [self.store.get(family, v) for v in versions]\n    else:\n        assert status is not None  # narrowed by the guards above\n        statuses = [Status(status)] if isinstance(status, str) else [Status(s) for s in status]\n        regs = self.store.by_status(family, statuses, as_of=as_of)\n        if not regs:\n            raise LookupError(f\"no {family!r} versions match status(es) {status!r}\")\n\n    specs = [r.spec for r in regs]\n    if len(specs) == 1 and version is not None:\n        # Single explicit version: don't attach a `version` column, keep\n        # the surface minimal.\n        return plan_for_spec(self.spark, specs[0], metrics=metrics)\n    return plan_for_specs(self.spark, specs, metrics=metrics)\n</code></pre>"},{"location":"reference/paths/","title":"Paths","text":"<p>Default filesystem locations for local dev state. All paths are anchored under <code>&lt;repo&gt;/.causalops/</code> so each checkout is self-contained.</p>"},{"location":"reference/paths/#causalops.paths.find_repo_root","title":"find_repo_root","text":"<pre><code>find_repo_root(start: Path | None = None) -&gt; Path\n</code></pre> <p>Walk up from <code>start</code> (or cwd) for a <code>pyproject.toml</code>; fall back to cwd.</p> Source code in <code>src/causalops/paths.py</code> <pre><code>def find_repo_root(start: Path | None = None) -&gt; Path:\n    \"\"\"Walk up from `start` (or cwd) for a `pyproject.toml`; fall back to cwd.\"\"\"\n    start = (start or Path.cwd()).resolve()\n    for candidate in (start, *start.parents):\n        if (candidate / \"pyproject.toml\").is_file():\n            return candidate\n    return Path.cwd().resolve()\n</code></pre>"},{"location":"reference/paths/#causalops.paths.default_data_dir","title":"default_data_dir","text":"<pre><code>default_data_dir() -&gt; Path\n</code></pre> <p>Where the seed script writes mock Parquet tables (and where specs point).</p> Source code in <code>src/causalops/paths.py</code> <pre><code>def default_data_dir() -&gt; Path:\n    \"\"\"Where the seed script writes mock Parquet tables (and where specs point).\"\"\"\n    return find_repo_root() / \".causalops\" / \"data\"\n</code></pre>"},{"location":"reference/paths/#causalops.paths.default_registry_json_path","title":"default_registry_json_path","text":"<pre><code>default_registry_json_path() -&gt; Path\n</code></pre> <p>Where the JSON-file SpecStore keeps its registrations + status log.</p> Source code in <code>src/causalops/paths.py</code> <pre><code>def default_registry_json_path() -&gt; Path:\n    \"\"\"Where the JSON-file SpecStore keeps its registrations + status log.\"\"\"\n    return find_repo_root() / \".causalops\" / \"registry.json\"\n</code></pre>"},{"location":"reference/planner/","title":"Planner","text":"<p>Resolves metric names against specs, reads physical tables via Spark, and joins or unions the results. The planner is the bridge between the declarative spec and the actual data.</p>"},{"location":"reference/planner/#causalops.planner.plan_for_spec","title":"plan_for_spec","text":"<pre><code>plan_for_spec(\n    spark: SparkSession,\n    spec: ModelSpec,\n    *,\n    metrics: Iterable[str],\n    include_columns: Iterable[str] = (),\n) -&gt; DataFrame\n</code></pre> <p>Resolve requested metric names against one spec and return a joined DataFrame.</p> <p>Group requested names by their owning table, select physical columns aliased to their canonical names, then outer-join across tables on the measurement key.</p> <p><code>include_columns</code> are extra physical columns (e.g. <code>run_date</code>, <code>channel_id</code>) passed through from the first table that contains them.</p> Source code in <code>src/causalops/planner.py</code> <pre><code>def plan_for_spec(\n    spark: SparkSession,\n    spec: ModelSpec,\n    *,\n    metrics: Iterable[str],\n    include_columns: Iterable[str] = (),\n) -&gt; DataFrame:\n    \"\"\"Resolve requested metric names against one spec and return a joined DataFrame.\n\n    Group requested names by their owning table, select physical columns aliased\n    to their canonical names, then outer-join across tables on the measurement key.\n\n    ``include_columns`` are extra physical columns (e.g. ``run_date``,\n    ``channel_id``) passed through from the first table that contains them.\n    \"\"\"\n    by_table: dict[Table, list[tuple[str, str]]] = defaultdict(list)\n    for name in metrics:\n        tbl, canonical, column = spec.resolve_metric(name)\n        by_table[tbl].append((canonical, column))\n\n    extra = list(include_columns)\n    extras_placed = False\n\n    per_table: list[DataFrame] = []\n    for tbl, cols in by_table.items():\n        select = [F.col(tbl.key).alias(spec.measurement_key)]\n        select += [F.col(col).alias(canonical) for canonical, col in cols]\n        raw = read_table(spark, tbl.path)\n        if extra and not extras_placed:\n            available = [c for c in extra if c in raw.columns]\n            select += [F.col(c) for c in available]\n            extras_placed = True\n        per_table.append(raw.select(*select))\n\n    joined = per_table[0]\n    for df in per_table[1:]:\n        joined = joined.join(df, on=spec.measurement_key, how=\"outer\")\n    return joined\n</code></pre>"},{"location":"reference/planner/#causalops.planner.plan_for_specs","title":"plan_for_specs","text":"<pre><code>plan_for_specs(\n    spark: SparkSession,\n    specs: Iterable[ModelSpec],\n    *,\n    metrics: Iterable[str],\n) -&gt; DataFrame\n</code></pre> <p>Plan per spec, tag with <code>version</code>, union with NULL padding for missing metrics.</p> Source code in <code>src/causalops/planner.py</code> <pre><code>def plan_for_specs(\n    spark: SparkSession,\n    specs: Iterable[ModelSpec],\n    *,\n    metrics: Iterable[str],\n) -&gt; DataFrame:\n    \"\"\"Plan per spec, tag with `version`, union with NULL padding for missing metrics.\"\"\"\n    wanted = list(metrics)\n    parts: list[DataFrame] = []\n    for spec in specs:\n        present = []\n        for m in wanted:\n            try:\n                spec.resolve_metric(m)\n                present.append(m)\n            except KeyError:\n                continue\n        if present:\n            part = plan_for_spec(spark, spec, metrics=present)\n        else:\n            part = spark.createDataFrame([], schema=f\"{spec.measurement_key} STRING\")\n        for m in wanted:\n            if m not in part.columns:\n                part = part.withColumn(m, F.lit(None).cast(\"double\"))\n        part = part.withColumn(\"version\", F.lit(spec.version))\n        parts.append(part.select(spec.measurement_key, *wanted, \"version\"))\n\n    out = parts[0]\n    for p in parts[1:]:\n        out = out.unionByName(p, allowMissingColumns=True)\n    return out\n</code></pre>"},{"location":"reference/spec/","title":"Spec Models","text":"<p>Frozen Pydantic models that describe a versioned contract for one model family's result tables. These are the core data structures that producers define and consumers query against.</p>"},{"location":"reference/spec/#dtype","title":"Dtype","text":""},{"location":"reference/spec/#causalops.spec.Dtype","title":"Dtype  <code>module-attribute</code>","text":"<pre><code>Dtype = Literal[\n    \"double\",\n    \"float\",\n    \"int\",\n    \"bigint\",\n    \"string\",\n    \"boolean\",\n    \"timestamp\",\n]\n</code></pre>"},{"location":"reference/spec/#metric","title":"Metric","text":""},{"location":"reference/spec/#causalops.spec.Metric","title":"Metric","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single metric column exposed under a canonical name.</p> <ul> <li><code>name</code> is the consumer-facing canonical name.</li> <li><code>column</code> is the physical column in the result table.</li> <li><code>aliases</code> are former canonical names still queryable for continuity.</li> </ul>"},{"location":"reference/spec/#causalops.spec.Metric.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str = Field(min_length=1)\n</code></pre>"},{"location":"reference/spec/#causalops.spec.Metric.column","title":"column  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>column: str = Field(min_length=1)\n</code></pre>"},{"location":"reference/spec/#causalops.spec.Metric.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: Dtype\n</code></pre>"},{"location":"reference/spec/#causalops.spec.Metric.aliases","title":"aliases  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>aliases: tuple[str, ...] = ()\n</code></pre>"},{"location":"reference/spec/#table","title":"Table","text":""},{"location":"reference/spec/#causalops.spec.Table","title":"Table","text":"<p>               Bases: <code>BaseModel</code></p> <p>A physical result table storing one or more metrics.</p>"},{"location":"reference/spec/#causalops.spec.Table.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str = Field(min_length=1)\n</code></pre>"},{"location":"reference/spec/#causalops.spec.Table.path","title":"path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>path: str = Field(\n    min_length=1,\n    description=\"Fully-qualified table identifier, e.g. catalog.schema.table\",\n)\n</code></pre>"},{"location":"reference/spec/#causalops.spec.Table.key","title":"key  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>key: str = Field(\n    min_length=1, description=\"Measurement key column name\"\n)\n</code></pre>"},{"location":"reference/spec/#causalops.spec.Table.metrics","title":"metrics  <code>instance-attribute</code>","text":"<pre><code>metrics: tuple[Metric, ...]\n</code></pre>"},{"location":"reference/spec/#modelspec","title":"ModelSpec","text":""},{"location":"reference/spec/#causalops.spec.ModelSpec","title":"ModelSpec","text":"<p>               Bases: <code>BaseModel</code></p> <p>A versioned contract for one model family's result tables.</p>"},{"location":"reference/spec/#causalops.spec.ModelSpec.family","title":"family  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>family: str = Field(min_length=1)\n</code></pre>"},{"location":"reference/spec/#causalops.spec.ModelSpec.version","title":"version  <code>instance-attribute</code>","text":"<pre><code>version: str\n</code></pre>"},{"location":"reference/spec/#causalops.spec.ModelSpec.measurement_key","title":"measurement_key  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>measurement_key: str = Field(min_length=1)\n</code></pre>"},{"location":"reference/spec/#causalops.spec.ModelSpec.tables","title":"tables  <code>instance-attribute</code>","text":"<pre><code>tables: tuple[Table, ...]\n</code></pre>"},{"location":"reference/spec/#causalops.spec.ModelSpec.resolve_metric","title":"resolve_metric","text":"<pre><code>resolve_metric(lookup: str) -&gt; tuple[Table, str, str]\n</code></pre> <p>Resolve a canonical name or alias to (Table, canonical_name, physical_column).</p> Source code in <code>src/causalops/spec.py</code> <pre><code>def resolve_metric(self, lookup: str) -&gt; tuple[Table, str, str]:\n    \"\"\"Resolve a canonical name or alias to (Table, canonical_name, physical_column).\"\"\"\n    for t in self.tables:\n        for m in t.metrics:\n            if lookup == m.name or lookup in m.aliases:\n                return t, m.name, m.column\n    raise KeyError(f\"unknown metric {lookup!r} in spec {self.family}@{self.version}\")\n</code></pre>"},{"location":"reference/store/","title":"Store","text":"<p>The storage layer for the model registry. Defines the abstract contract (<code>SpecStore</code>), the lifecycle status enum, the registration and event data classes, a concrete JSON-file backend, and a factory function.</p>"},{"location":"reference/store/#status","title":"Status","text":""},{"location":"reference/store/#causalops.store.base.Status","title":"Status","text":"<p>               Bases: <code>StrEnum</code></p>"},{"location":"reference/store/#registration","title":"Registration","text":""},{"location":"reference/store/#causalops.store.base.Registration","title":"Registration  <code>dataclass</code>","text":"<pre><code>Registration(\n    spec: ModelSpec,\n    git_repo: str,\n    git_tag: str,\n    git_sha: str,\n    sdk_version: str,\n    registered_by: str,\n    registered_at: datetime,\n)\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.spec","title":"spec  <code>instance-attribute</code>","text":"<pre><code>spec: ModelSpec\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.git_repo","title":"git_repo  <code>instance-attribute</code>","text":"<pre><code>git_repo: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.git_tag","title":"git_tag  <code>instance-attribute</code>","text":"<pre><code>git_tag: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.git_sha","title":"git_sha  <code>instance-attribute</code>","text":"<pre><code>git_sha: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.sdk_version","title":"sdk_version  <code>instance-attribute</code>","text":"<pre><code>sdk_version: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.registered_by","title":"registered_by  <code>instance-attribute</code>","text":"<pre><code>registered_by: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.registered_at","title":"registered_at  <code>instance-attribute</code>","text":"<pre><code>registered_at: datetime\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.family","title":"family  <code>property</code>","text":"<pre><code>family: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.Registration.version","title":"version  <code>property</code>","text":"<pre><code>version: str\n</code></pre>"},{"location":"reference/store/#statusevent","title":"StatusEvent","text":""},{"location":"reference/store/#causalops.store.base.StatusEvent","title":"StatusEvent  <code>dataclass</code>","text":"<pre><code>StatusEvent(\n    event_id: str,\n    family: str,\n    version: str,\n    status: Status,\n    effective_from: datetime,\n    assigned_by: str,\n    note: str,\n)\n</code></pre>"},{"location":"reference/store/#causalops.store.base.StatusEvent.event_id","title":"event_id  <code>instance-attribute</code>","text":"<pre><code>event_id: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.StatusEvent.family","title":"family  <code>instance-attribute</code>","text":"<pre><code>family: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.StatusEvent.version","title":"version  <code>instance-attribute</code>","text":"<pre><code>version: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.StatusEvent.status","title":"status  <code>instance-attribute</code>","text":"<pre><code>status: Status\n</code></pre>"},{"location":"reference/store/#causalops.store.base.StatusEvent.effective_from","title":"effective_from  <code>instance-attribute</code>","text":"<pre><code>effective_from: datetime\n</code></pre>"},{"location":"reference/store/#causalops.store.base.StatusEvent.assigned_by","title":"assigned_by  <code>instance-attribute</code>","text":"<pre><code>assigned_by: str\n</code></pre>"},{"location":"reference/store/#causalops.store.base.StatusEvent.note","title":"note  <code>instance-attribute</code>","text":"<pre><code>note: str\n</code></pre>"},{"location":"reference/store/#specstore","title":"SpecStore","text":""},{"location":"reference/store/#causalops.store.base.SpecStore","title":"SpecStore","text":"<p>               Bases: <code>ABC</code></p> <p>Two-table registry storage.</p>"},{"location":"reference/store/#causalops.store.base.SpecStore.put","title":"put  <code>abstractmethod</code>","text":"<pre><code>put(\n    spec: ModelSpec,\n    *,\n    git_repo: str,\n    git_tag: str,\n    git_sha: str,\n    registered_by: str,\n    sdk_version: str,\n    registered_at: datetime | None = None,\n) -&gt; Registration\n</code></pre> <p>Insert a registration and its initial <code>experiment</code> status event.</p> <p><code>registered_at</code> is the timestamp stamped on both the registration and its initial status event; defaults to <code>datetime.now(UTC)</code>. Overriding it lets seed scripts align the status log with a mock timeline.</p> <p>Raises <code>KeyError</code> if (family, version) already exists (callers use <code>exists</code> first when they want to expose a --force flag).</p> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef put(\n    self,\n    spec: ModelSpec,\n    *,\n    git_repo: str,\n    git_tag: str,\n    git_sha: str,\n    registered_by: str,\n    sdk_version: str,\n    registered_at: datetime | None = None,\n) -&gt; Registration:\n    \"\"\"Insert a registration and its initial `experiment` status event.\n\n    `registered_at` is the timestamp stamped on both the registration and\n    its initial status event; defaults to `datetime.now(UTC)`. Overriding\n    it lets seed scripts align the status log with a mock timeline.\n\n    Raises `KeyError` if (family, version) already exists (callers use\n    `exists` first when they want to expose a --force flag).\"\"\"\n</code></pre>"},{"location":"reference/store/#causalops.store.base.SpecStore.exists","title":"exists  <code>abstractmethod</code>","text":"<pre><code>exists(family: str, version: str) -&gt; bool\n</code></pre> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef exists(self, family: str, version: str) -&gt; bool: ...\n</code></pre>"},{"location":"reference/store/#causalops.store.base.SpecStore.get","title":"get  <code>abstractmethod</code>","text":"<pre><code>get(family: str, version: str) -&gt; Registration\n</code></pre> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef get(self, family: str, version: str) -&gt; Registration: ...\n</code></pre>"},{"location":"reference/store/#causalops.store.base.SpecStore.list_families","title":"list_families  <code>abstractmethod</code>","text":"<pre><code>list_families() -&gt; list[str]\n</code></pre> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef list_families(self) -&gt; list[str]: ...\n</code></pre>"},{"location":"reference/store/#causalops.store.base.SpecStore.list_versions","title":"list_versions  <code>abstractmethod</code>","text":"<pre><code>list_versions(family: str) -&gt; list[str]\n</code></pre> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef list_versions(self, family: str) -&gt; list[str]: ...\n</code></pre>"},{"location":"reference/store/#causalops.store.base.SpecStore.promote","title":"promote  <code>abstractmethod</code>","text":"<pre><code>promote(\n    family: str,\n    version: str,\n    status: Status,\n    *,\n    assigned_by: str,\n    note: str = \"\",\n    reactivate: bool = False,\n    effective_from: datetime | None = None,\n) -&gt; None\n</code></pre> <p>Append a status event. Enforces:</p> <ul> <li><code>production</code> promotion is atomic with retirement of the current prod.</li> <li>Un-retiring (retired -&gt; anything) requires <code>reactivate=True</code>.</li> </ul> <p><code>effective_from</code> stamps the new status event (and any auto-retire it cascades in the same commit); defaults to <code>datetime.now(UTC)</code>.</p> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef promote(\n    self,\n    family: str,\n    version: str,\n    status: Status,\n    *,\n    assigned_by: str,\n    note: str = \"\",\n    reactivate: bool = False,\n    effective_from: datetime | None = None,\n) -&gt; None:\n    \"\"\"Append a status event. Enforces:\n\n    - `production` promotion is atomic with retirement of the current prod.\n    - Un-retiring (retired -&gt; anything) requires `reactivate=True`.\n\n    `effective_from` stamps the new status event (and any auto-retire it\n    cascades in the same commit); defaults to `datetime.now(UTC)`.\n    \"\"\"\n</code></pre>"},{"location":"reference/store/#causalops.store.base.SpecStore.current_status","title":"current_status  <code>abstractmethod</code>","text":"<pre><code>current_status(\n    family: str,\n    version: str,\n    *,\n    as_of: datetime | None = None,\n) -&gt; Status\n</code></pre> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef current_status(\n    self,\n    family: str,\n    version: str,\n    *,\n    as_of: datetime | None = None,\n) -&gt; Status: ...\n</code></pre>"},{"location":"reference/store/#causalops.store.base.SpecStore.by_status","title":"by_status  <code>abstractmethod</code>","text":"<pre><code>by_status(\n    family: str,\n    status: Status | list[Status],\n    *,\n    as_of: datetime | None = None,\n) -&gt; list[Registration]\n</code></pre> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef by_status(\n    self,\n    family: str,\n    status: Status | list[Status],\n    *,\n    as_of: datetime | None = None,\n) -&gt; list[Registration]: ...\n</code></pre>"},{"location":"reference/store/#causalops.store.base.SpecStore.history","title":"history  <code>abstractmethod</code>","text":"<pre><code>history(family: str, version: str) -&gt; list[StatusEvent]\n</code></pre> Source code in <code>src/causalops/store/base.py</code> <pre><code>@abstractmethod\ndef history(self, family: str, version: str) -&gt; list[StatusEvent]: ...\n</code></pre>"},{"location":"reference/store/#jsonfilespecstore","title":"JsonFileSpecStore","text":""},{"location":"reference/store/#causalops.store.json_file.JsonFileSpecStore","title":"JsonFileSpecStore  <code>dataclass</code>","text":"<pre><code>JsonFileSpecStore(path: Path)\n</code></pre> <p>               Bases: <code>SpecStore</code></p>"},{"location":"reference/store/#causalops.store.json_file.JsonFileSpecStore.delete","title":"delete","text":"<pre><code>delete(family, version)\n</code></pre> <p>Remove a registration (used by CLI --force). Not on the ABC.</p> <p>Leaves the status_log intact so history() still reflects what happened.</p> Source code in <code>src/causalops/store/json_file.py</code> <pre><code>def delete(self, family, version):\n    \"\"\"Remove a registration (used by CLI --force). Not on the ABC.\n\n    Leaves the status_log intact so history() still reflects what happened.\n    \"\"\"\n    data = self._load()\n    before = len(data[\"registrations\"])\n    data[\"registrations\"] = [\n        r\n        for r in data[\"registrations\"]\n        if not (r[\"family\"] == family and r[\"version\"] == version)\n    ]\n    if len(data[\"registrations\"]) != before:\n        self._dump(data)\n</code></pre>"},{"location":"reference/store/#get_store","title":"get_store","text":""},{"location":"reference/store/#causalops.store.get_store","title":"get_store","text":"<pre><code>get_store() -&gt; SpecStore\n</code></pre> <p>Build the configured SpecStore.</p> <p>Reads <code>CAUSALOPS_STORE_CONFIG</code> (JSON) from the environment, e.g.</p> <pre><code>{\"backend\": \"json_file\", \"path\": \".causalops/registry.json\"}\n</code></pre> <p>Defaults to a <code>json_file</code> backend at <code>&lt;repo&gt;/.causalops/registry.json</code>.</p> Source code in <code>src/causalops/store/__init__.py</code> <pre><code>def get_store() -&gt; SpecStore:\n    \"\"\"Build the configured SpecStore.\n\n    Reads `CAUSALOPS_STORE_CONFIG` (JSON) from the environment, e.g.\n\n        {\"backend\": \"json_file\", \"path\": \".causalops/registry.json\"}\n\n    Defaults to a `json_file` backend at `&lt;repo&gt;/.causalops/registry.json`.\n    \"\"\"\n    raw = os.environ.get(\"CAUSALOPS_STORE_CONFIG\")\n    if raw:\n        cfg = json.loads(raw)\n    else:\n        cfg = {\"backend\": \"json_file\", \"path\": str(default_registry_json_path())}\n    backend = cfg.get(\"backend\", \"json_file\")\n    if backend == \"json_file\":\n        return JsonFileSpecStore(path=cfg[\"path\"])\n    raise ValueError(f\"unsupported backend {backend!r}\")\n</code></pre>"},{"location":"reference/utils/","title":"Utilities","text":"<p>Spark session factory and table reader. <code>build_local_spark_session</code> creates a minimal local session for development; <code>read_table</code> dispatches between filesystem paths and catalog identifiers.</p>"},{"location":"reference/utils/#causalops.utils.build_local_spark_session","title":"build_local_spark_session","text":"<pre><code>build_local_spark_session(\n    *, app_name: str = \"causalops\", master: str = \"local[2]\"\n) -&gt; SparkSession\n</code></pre> <p>Build (or reuse) a local Spark session for reading Parquet.</p> <p>Spark only allows one active session per JVM, so callers that need isolation should share the session-scoped pytest fixture rather than building fresh ones.</p> Source code in <code>src/causalops/utils.py</code> <pre><code>def build_local_spark_session(\n    *,\n    app_name: str = \"causalops\",\n    master: str = \"local[2]\",\n) -&gt; SparkSession:\n    \"\"\"Build (or reuse) a local Spark session for reading Parquet.\n\n    Spark only allows one active session per JVM, so callers that need\n    isolation should share the session-scoped pytest fixture rather than\n    building fresh ones.\n    \"\"\"\n    from pyspark.sql import SparkSession\n\n    if master.startswith(\"local\"):\n        os.environ.pop(\"SPARK_HOME\", None)\n        os.environ.setdefault(\"PYSPARK_PYTHON\", sys.executable)\n        os.environ.setdefault(\"PYSPARK_DRIVER_PYTHON\", sys.executable)\n        os.environ.setdefault(\"SPARK_LOCAL_IP\", \"127.0.0.1\")\n        os.environ.setdefault(\n            \"PYSPARK_SUBMIT_ARGS\",\n            shlex.join(\n                [\n                    \"--driver-java-options\",\n                    f\"-Dlog4j2.configurationFile={_LOG4J_CONFIG}\",\n                    \"pyspark-shell\",\n                ]\n            ),\n        )\n\n    spark = (\n        SparkSession.builder.appName(app_name)\n        .master(master)\n        .config(\"spark.sql.shuffle.partitions\", \"2\")\n        .config(\"spark.ui.enabled\", \"false\")\n        .config(\"spark.ui.showConsoleProgress\", \"false\")\n        .getOrCreate()\n    )\n    spark.sparkContext.setLogLevel(\"ERROR\")\n    return spark\n</code></pre>"},{"location":"reference/utils/#causalops.utils.read_table","title":"read_table","text":"<pre><code>read_table(spark: SparkSession, path: str) -&gt; DataFrame\n</code></pre> <p>Read a spec-declared table as a DataFrame (Parquet path or catalog name).</p> Source code in <code>src/causalops/utils.py</code> <pre><code>def read_table(spark: SparkSession, path: str) -&gt; DataFrame:\n    \"\"\"Read a spec-declared table as a DataFrame (Parquet path or catalog name).\"\"\"\n    if _looks_like_filesystem_path(path):\n        return spark.read.parquet(path)\n    return spark.table(path)\n</code></pre>"},{"location":"reference/validation/","title":"Validation","text":"<p>Validates a spec's declared tables and columns against the live catalog or Parquet files. Missing tables produce warnings (expected before a pipeline's first run); missing columns or dtype mismatches produce errors.</p>"},{"location":"reference/validation/#validationreport","title":"ValidationReport","text":""},{"location":"reference/validation/#causalops.validation.ValidationReport","title":"ValidationReport  <code>dataclass</code>","text":"<pre><code>ValidationReport(\n    errors: list[str] = list(), warnings: list[str] = list()\n)\n</code></pre> <p>Accumulated errors and warnings from validating a spec against live tables.</p>"},{"location":"reference/validation/#causalops.validation.ValidationReport.errors","title":"errors  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>errors: list[str] = field(default_factory=list)\n</code></pre>"},{"location":"reference/validation/#causalops.validation.ValidationReport.warnings","title":"warnings  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>warnings: list[str] = field(default_factory=list)\n</code></pre>"},{"location":"reference/validation/#causalops.validation.ValidationReport.has_errors","title":"has_errors  <code>property</code>","text":"<pre><code>has_errors: bool\n</code></pre> <p>True if any hard errors (missing columns, dtype mismatches) were found.</p>"},{"location":"reference/validation/#causalops.validation.ValidationReport.has_warnings","title":"has_warnings  <code>property</code>","text":"<pre><code>has_warnings: bool\n</code></pre> <p>True if any warnings (e.g. table not yet created) were found.</p>"},{"location":"reference/validation/#causalops.validation.ValidationReport.format","title":"format","text":"<pre><code>format() -&gt; str\n</code></pre> <p>Render all errors and warnings as a human-readable string.</p> Source code in <code>src/causalops/validation.py</code> <pre><code>def format(self) -&gt; str:\n    \"\"\"Render all errors and warnings as a human-readable string.\"\"\"\n    parts = []\n    if self.errors:\n        parts.append(\"Errors:\\n  \" + \"\\n  \".join(self.errors))\n    if self.warnings:\n        parts.append(\"Warnings:\\n  \" + \"\\n  \".join(self.warnings))\n    return \"\\n\".join(parts)\n</code></pre>"},{"location":"reference/validation/#causalops.validation.ValidationReport.format_warnings","title":"format_warnings","text":"<pre><code>format_warnings() -&gt; str\n</code></pre> <p>Render only warnings as a human-readable string.</p> Source code in <code>src/causalops/validation.py</code> <pre><code>def format_warnings(self) -&gt; str:\n    \"\"\"Render only warnings as a human-readable string.\"\"\"\n    return \"Warnings:\\n  \" + \"\\n  \".join(self.warnings)\n</code></pre>"},{"location":"reference/validation/#validate_against_uc","title":"validate_against_uc","text":""},{"location":"reference/validation/#causalops.validation.validate_against_uc","title":"validate_against_uc","text":"<pre><code>validate_against_uc(\n    spec: ModelSpec, *, spark: SparkSession\n) -&gt; ValidationReport\n</code></pre> Source code in <code>src/causalops/validation.py</code> <pre><code>def validate_against_uc(spec: ModelSpec, *, spark: SparkSession) -&gt; ValidationReport:\n    report = ValidationReport()\n    for table in spec.tables:\n        try:\n            actual = {\n                f.name: f.dataType.simpleString()\n                for f in read_table(spark, table.path).schema.fields\n            }\n        except Exception:\n            report.warnings.append(f\"{table.path} does not exist yet (first registration?)\")\n            continue\n        for m in table.metrics:\n            if m.column not in actual:\n                report.errors.append(\n                    f\"{table.path} missing column {m.column!r} (declared for metric {m.name!r})\"\n                )\n            elif actual[m.column] != m.dtype:\n                report.errors.append(\n                    f\"{table.path}.{m.column}: spec says {m.dtype}, table has {actual[m.column]}\"\n                )\n    return report\n</code></pre>"},{"location":"tutorials/","title":"Tutorials","text":"<p>These four tutorials walk you through causalops end-to-end, from defining your first model spec to querying results downstream. They are designed to be followed in order \u2014 each builds on the state created by the previous one.</p> # Tutorial Persona 1 Define a ModelSpec Model producer 2 Register &amp; Validate Model producer 3 Promote through the Lifecycle Platform team 4 Query Results Consumer / downstream analytics <p>Before you start</p> <p>Install causalops and its dependencies:</p> <pre><code>pip install -e '.[dev]'\n</code></pre> <p>You will also need Java 17 for the local Spark session:</p> <pre><code>export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64  # or your Java 17 path\n</code></pre>"},{"location":"tutorials/define-a-model-spec/","title":"Define a ModelSpec","text":"<p>In this tutorial you will create a model spec \u2014 the versioned contract that declares which tables and metrics a causal inference model produces.</p> <p>Audience: Model producer (the team that owns the model).</p> <p>Prerequisites: <code>pip install causalops</code></p>"},{"location":"tutorials/define-a-model-spec/#what-is-a-modelspec","title":"What is a ModelSpec?","text":"<p>A <code>ModelSpec</code> is a frozen Pydantic model that describes one version of a model family's result tables. It declares:</p> <ul> <li>The family name (e.g. <code>\"uplift\"</code>, <code>\"scm\"</code>, <code>\"bsts\"</code>).</li> <li>A semver version like <code>\"3.1.0\"</code>.</li> <li>A measurement key that joins rows across tables.</li> <li>One or more tables, each containing metrics that map consumer-facing   canonical names to physical columns.</li> </ul>"},{"location":"tutorials/define-a-model-spec/#step-1-create-a-spec-file","title":"Step 1: Create a spec file","text":"<p>Create a file called <code>model_spec.py</code>. This is the file that the <code>causalops</code> CLI will load when you register or validate.</p> <pre><code>from causalops import Metric, ModelSpec, Table\n</code></pre>"},{"location":"tutorials/define-a-model-spec/#step-2-define-metrics","title":"Step 2: Define metrics","text":"<p>Each <code>Metric</code> maps a canonical name to a physical column in a result table:</p> <pre><code>treatment_effect = Metric(\n    name=\"treatment_effect\",   # consumer-facing canonical name\n    column=\"ate\",              # physical column in the Parquet/UC table\n    dtype=\"double\",\n    aliases=(\"te\",),           # former names still queryable\n)\nci_lower = Metric(name=\"ci_lower\", column=\"ci_lo\", dtype=\"double\")\nci_upper = Metric(name=\"ci_upper\", column=\"ci_hi\", dtype=\"double\")\n</code></pre> <p>The <code>aliases</code> tuple lets you rename a metric across versions without breaking downstream queries \u2014 consumers who still request <code>\"te\"</code> will get the column named <code>\"treatment_effect\"</code>.</p>"},{"location":"tutorials/define-a-model-spec/#step-3-group-metrics-into-tables","title":"Step 3: Group metrics into tables","text":"<p>A <code>Table</code> groups related metrics and points at the physical data:</p> <pre><code>shared_table = Table(\n    name=\"shared\",\n    path=\".causalops/data/uplift/shared_v3.1.0.parquet\",\n    key=\"experiment_id\",\n    metrics=(treatment_effect, ci_lower, ci_upper),\n)\n\nheterogeneity_table = Table(\n    name=\"heterogeneity\",\n    path=\".causalops/data/uplift/het_v3.1.0.parquet\",\n    key=\"experiment_id\",\n    metrics=(\n        Metric(\n            name=\"cate_variance\",\n            column=\"het_score\",\n            dtype=\"double\",\n            aliases=(\"heterogeneity_score\",),\n        ),\n    ),\n)\n</code></pre> <p>Note</p> <p><code>path</code> is a filesystem path in local dev and a Unity Catalog identifier (e.g. <code>main.uplift.shared_v3</code>) on Databricks. The planner handles both transparently.</p>"},{"location":"tutorials/define-a-model-spec/#step-4-compose-the-modelspec","title":"Step 4: Compose the ModelSpec","text":"<pre><code>spec = ModelSpec(\n    family=\"uplift\",\n    version=\"3.1.0\",\n    measurement_key=\"experiment_id\",\n    tables=(shared_table, heterogeneity_table),\n)\n</code></pre> <p>The complete <code>model_spec.py</code> should define a top-level variable called <code>spec</code> \u2014 this is what the CLI loads.</p> Full model_spec.py <pre><code>from causalops import Metric, ModelSpec, Table\n\nspec = ModelSpec(\n    family=\"uplift\",\n    version=\"3.1.0\",\n    measurement_key=\"experiment_id\",\n    tables=(\n        Table(\n            name=\"shared\",\n            path=\".causalops/data/uplift/shared_v3.1.0.parquet\",\n            key=\"experiment_id\",\n            metrics=(\n                Metric(\n                    name=\"treatment_effect\",\n                    column=\"ate\",\n                    dtype=\"double\",\n                    aliases=(\"te\",),\n                ),\n                Metric(name=\"ci_lower\", column=\"ci_lo\", dtype=\"double\"),\n                Metric(name=\"ci_upper\", column=\"ci_hi\", dtype=\"double\"),\n            ),\n        ),\n        Table(\n            name=\"heterogeneity\",\n            path=\".causalops/data/uplift/het_v3.1.0.parquet\",\n            key=\"experiment_id\",\n            metrics=(\n                Metric(\n                    name=\"cate_variance\",\n                    column=\"het_score\",\n                    dtype=\"double\",\n                    aliases=(\"heterogeneity_score\",),\n                ),\n            ),\n        ),\n    ),\n)\n</code></pre>"},{"location":"tutorials/define-a-model-spec/#step-5-explore-validation-rules","title":"Step 5: Explore validation rules","text":"<p>causalops validates your spec at construction time. Here are the most common errors you will see:</p>"},{"location":"tutorials/define-a-model-spec/#invalid-semver","title":"Invalid semver","text":"<p>The version must be three-part <code>MAJOR.MINOR.PATCH</code>:</p> <pre><code>ModelSpec(family=\"uplift\", version=\"v3.1\", measurement_key=\"eid\", tables=())\n# ValidationError: version 'v3.1' must be MAJOR.MINOR.PATCH (e.g. 3.1.0)\n</code></pre>"},{"location":"tutorials/define-a-model-spec/#duplicate-metric-names","title":"Duplicate metric names","text":"<p>Two metrics in the same table cannot share a canonical name:</p> <pre><code>Table(\n    name=\"t\",\n    path=\"p\",\n    key=\"k\",\n    metrics=(\n        Metric(name=\"x\", column=\"a\", dtype=\"double\"),\n        Metric(name=\"x\", column=\"b\", dtype=\"double\"),\n    ),\n)\n# ValidationError: duplicate metric 'x' in table 't'\n</code></pre>"},{"location":"tutorials/define-a-model-spec/#alias-collides-with-canonical-name","title":"Alias collides with canonical name","text":"<p>An alias cannot match another metric's canonical name within the same table:</p> <pre><code>Table(\n    name=\"t\",\n    path=\"p\",\n    key=\"k\",\n    metrics=(\n        Metric(name=\"x\", column=\"a\", dtype=\"double\"),\n        Metric(name=\"y\", column=\"b\", dtype=\"double\", aliases=(\"x\",)),\n    ),\n)\n# ValidationError: alias 'x' collides with an existing metric name or alias in table 't'\n</code></pre>"},{"location":"tutorials/define-a-model-spec/#cross-table-collision","title":"Cross-table collision","text":"<p>A metric lookup name must be unique across all tables in the spec:</p> <pre><code>ModelSpec(\n    family=\"f\",\n    version=\"1.0.0\",\n    measurement_key=\"k\",\n    tables=(\n        Table(name=\"a\", path=\"p1\", key=\"k\", metrics=(Metric(name=\"x\", column=\"c1\", dtype=\"double\"),)),\n        Table(name=\"b\", path=\"p2\", key=\"k\", metrics=(Metric(name=\"x\", column=\"c2\", dtype=\"double\"),)),\n    ),\n)\n# ValidationError: cross-table collision: metric lookup 'x' appears in tables 'a' and 'b'\n</code></pre>"},{"location":"tutorials/define-a-model-spec/#step-6-resolve-metrics","title":"Step 6: Resolve metrics","text":"<p>Use <code>resolve_metric()</code> to verify that a canonical name or alias resolves correctly:</p> <pre><code>table, canonical, column = spec.resolve_metric(\"te\")\nprint(table.name)   # \"shared\"\nprint(canonical)     # \"treatment_effect\"\nprint(column)        # \"ate\"\n</code></pre> <p>Unknown metrics raise <code>KeyError</code>:</p> <pre><code>spec.resolve_metric(\"nonexistent\")\n# KeyError: \"unknown metric 'nonexistent' in spec uplift@3.1.0\"\n</code></pre>"},{"location":"tutorials/define-a-model-spec/#step-7-json-round-trip","title":"Step 7: JSON round-trip","text":"<p>Specs are serializable \u2014 useful for storage and debugging:</p> <pre><code>json_str = spec.model_dump_json(indent=2)\nprint(json_str)\n\nrestored = ModelSpec.model_validate_json(json_str)\nassert restored == spec\n</code></pre> <p>Because all models are frozen (immutable), they are also hashable and can be used as dictionary keys.</p> <p>Next: Register &amp; Validate \u2014 take this spec through schema validation and register it in the local registry.</p>"},{"location":"tutorials/promote-lifecycle/","title":"Promote through the Lifecycle","text":"<p>In this tutorial you will move a registered model version through the causalops lifecycle: experiment, challenger, production, and retired.</p> <p>Audience: Platform team.</p> <p>Prerequisites: Register &amp; Validate complete \u2014 you have <code>uplift@3.1.0</code> registered with status <code>experiment</code>.</p>"},{"location":"tutorials/promote-lifecycle/#the-four-statuses","title":"The four statuses","text":"<p>Every registered version starts as <code>experiment</code> and progresses through a fixed set of statuses:</p> <pre><code>experiment \u2192 challenger \u2192 production \u2192 retired\n</code></pre> Status Meaning <code>experiment</code> Initial state after registration. Under development. <code>challenger</code> Shadow-deployed alongside the current production model. <code>production</code> The active production version. At most one per family. <code>retired</code> Replaced by a newer production version. <p>Note</p> <p>There is no restriction on which transitions are allowed (you can go from <code>experiment</code> straight to <code>production</code>), but the <code>retired</code> \u2192 anything transition requires the <code>--reactivate</code> flag as a safety guard.</p>"},{"location":"tutorials/promote-lifecycle/#step-1-promote-to-challenger","title":"Step 1: Promote to challenger","text":"<pre><code>causalops promote \\\n    --family uplift \\\n    --version 3.1.0 \\\n    --status challenger \\\n    --assigned-by \"$USER\"\n</code></pre> <p>Output:</p> <pre><code>Promoted: uplift@3.1.0 -&gt; challenger\n</code></pre> <p>You can optionally attach a note:</p> <pre><code>causalops promote \\\n    --family uplift \\\n    --version 3.1.0 \\\n    --status challenger \\\n    --assigned-by \"$USER\" \\\n    --note \"shadow deployment started\"\n</code></pre>"},{"location":"tutorials/promote-lifecycle/#step-2-promote-to-production","title":"Step 2: Promote to production","text":"<p>When the challenger has been validated, promote it to production:</p> <pre><code>causalops promote \\\n    --family uplift \\\n    --version 3.1.0 \\\n    --status production \\\n    --assigned-by \"$USER\" \\\n    --note \"graduated from shadow\"\n</code></pre>"},{"location":"tutorials/promote-lifecycle/#atomic-production-swap","title":"Atomic production swap","text":"<p>If another version of the same family is already in production, it is automatically retired in the same operation. Both status events share the same <code>effective_from</code> timestamp, ensuring there is never a moment where two versions claim production simultaneously.</p> <p>For example, if <code>uplift@3.0.0</code> was in production, promoting <code>uplift@3.1.0</code> to production atomically:</p> <ol> <li>Writes a <code>production</code> event for <code>uplift@3.1.0</code>.</li> <li>Writes a <code>retired</code> event for <code>uplift@3.0.0</code>.</li> </ol> <p>Both events have the same timestamp.</p>"},{"location":"tutorials/promote-lifecycle/#step-3-the-reactivate-guard","title":"Step 3: The reactivate guard","text":"<p>Once a version is retired, promoting it to any other status requires the <code>--reactivate</code> flag:</p> <pre><code># This fails:\ncausalops promote --family uplift --version 3.0.0 --status challenger --assigned-by \"$USER\"\n# Error: uplift@3.0.0 is retired; pass --reactivate to un-retire\n\n# This succeeds:\ncausalops promote --family uplift --version 3.0.0 --status challenger \\\n    --assigned-by \"$USER\" --reactivate\n</code></pre> <p>Warning</p> <p>Reactivating a retired version can silently change what downstream consumers see (e.g. if they query by status). The flag exists to make this a deliberate decision, not an accident.</p>"},{"location":"tutorials/promote-lifecycle/#step-4-query-history-from-python","title":"Step 4: Query history from Python","text":"<p>You can inspect the full status timeline programmatically:</p> <pre><code>from causalops import RegistryClient\nfrom causalops.store import get_store\nfrom causalops.utils import build_local_spark_session\n\nspark = build_local_spark_session()\nclient = RegistryClient(store=get_store(), spark=spark)\n\nfor event in client.history(\"uplift\", \"3.1.0\"):\n    print(f\"{event.effective_from}  {event.status.value:12s}  by {event.assigned_by}\")\n</code></pre> <p>Output:</p> <pre><code>2026-09-06 12:00:00+00:00  experiment    by alice\n2026-09-06 12:05:00+00:00  challenger    by alice\n2026-09-06 12:10:00+00:00  production    by alice\n</code></pre>"},{"location":"tutorials/promote-lifecycle/#step-5-query-production-windows","title":"Step 5: Query production windows","text":"<p><code>production_windows()</code> returns each version's production stints as <code>(version, start_date, end_date)</code> tuples:</p> <pre><code>for version, start, end in client.production_windows(\"uplift\"):\n    print(f\"v{version}  {start} .. {end or '(open)'}\")\n</code></pre> <p>Output:</p> <pre><code>v3.0.0  2026-08-01 .. 2026-09-06\nv3.1.0  2026-09-06 .. (open)\n</code></pre> <p>A production window opens on the <code>PRODUCTION</code> event and closes on the subsequent <code>RETIRED</code> event. If the version is still in production, the end date is <code>None</code>.</p> <p>Next: Query Results \u2014 use the RegistryClient to query model results by version or status.</p>"},{"location":"tutorials/query-results/","title":"Query Results","text":"<p>In this tutorial you will use the <code>RegistryClient</code> to discover registered models and query their results as Spark DataFrames with canonical column names.</p> <p>Audience: Consumer / downstream analytics.</p> <p>Prerequisites: Promote through the Lifecycle complete \u2014 you have at least one version registered and promoted to production.</p>"},{"location":"tutorials/query-results/#step-1-build-a-registryclient","title":"Step 1: Build a RegistryClient","text":"<pre><code>from causalops import RegistryClient\nfrom causalops.store import get_store\nfrom causalops.utils import build_local_spark_session\n\nspark = build_local_spark_session()\nclient = RegistryClient(store=get_store(), spark=spark)\n</code></pre> <p>The client needs two things: a <code>SpecStore</code> to read registrations and status events, and a <code>SparkSession</code> to read result tables.</p>"},{"location":"tutorials/query-results/#step-2-discover-what-is-registered","title":"Step 2: Discover what is registered","text":""},{"location":"tutorials/query-results/#list-families","title":"List families","text":"<pre><code>client.list_families()\n# ['uplift']\n</code></pre>"},{"location":"tutorials/query-results/#list-versions","title":"List versions","text":"<pre><code>client.list_versions(\"uplift\")\n# ['3.0.0', '3.1.0']\n</code></pre>"},{"location":"tutorials/query-results/#describe-a-version","title":"Describe a version","text":"<p><code>describe()</code> returns the full <code>Registration</code> record:</p> <pre><code>reg = client.describe(\"uplift\", \"3.1.0\")\nprint(reg.git_repo)       # 'local/uplift-model'\nprint(reg.registered_by)  # 'alice'\nprint(reg.spec.tables)    # the full spec with tables and metrics\n</code></pre>"},{"location":"tutorials/query-results/#view-status-history","title":"View status history","text":"<pre><code>for event in client.history(\"uplift\", \"3.1.0\"):\n    print(f\"{event.status.value:12s}  {event.effective_from}\")\n</code></pre>"},{"location":"tutorials/query-results/#step-3-query-by-explicit-version","title":"Step 3: Query by explicit version","text":"<p>Request specific metrics for a known version:</p> <pre><code>df = client.get_results(\n    family=\"uplift\",\n    version=\"3.1.0\",\n    metrics=[\"treatment_effect\", \"cate_variance\"],\n)\ndf.show(5)\n</code></pre> <pre><code>+-------------+----------------+-------------+\n|experiment_id|treatment_effect|cate_variance|\n+-------------+----------------+-------------+\n|      exp_001|          0.0412|       0.1523|\n|      exp_002|          0.0387|       0.0891|\n|      exp_003|          0.0445|       0.2104|\n+-------------+----------------+-------------+\n</code></pre> <p>The planner resolves each metric to its physical column, reads the relevant tables, and outer-joins them on the measurement key. The returned DataFrame uses canonical names as column headers, not the physical column names.</p>"},{"location":"tutorials/query-results/#step-4-query-by-status","title":"Step 4: Query by status","text":"<p>Instead of specifying a version, query by lifecycle status:</p> <pre><code>df = client.get_results(\n    family=\"uplift\",\n    status=\"production\",\n    metrics=[\"treatment_effect\", \"ci_lower\", \"ci_upper\"],\n)\ndf.show(5)\n</code></pre> <p>This returns results for whichever version is currently in production.</p> <p>Note</p> <p>You must pass either <code>version</code> or <code>status</code>, never both. Passing both raises <code>ValueError</code>.</p>"},{"location":"tutorials/query-results/#step-5-multi-status-union","title":"Step 5: Multi-status union","text":"<p>Query multiple statuses to compare production and challenger side-by-side:</p> <pre><code>df = client.get_results(\n    family=\"uplift\",\n    status=[\"production\", \"challenger\"],\n    metrics=[\"treatment_effect\", \"cate_variance\"],\n)\ndf.show(5)\n</code></pre> <pre><code>+-------------+----------------+-------------+-------+\n|experiment_id|treatment_effect|cate_variance|version|\n+-------------+----------------+-------------+-------+\n|      exp_001|          0.0412|       0.1523|  3.1.0|\n|      exp_001|          0.0390|         null|  3.0.0|\n+-------------+----------------+-------------+-------+\n</code></pre> <p>When multiple versions match, the result includes a <code>version</code> column to distinguish rows. Metrics that exist in one version but not the other are padded with <code>null</code>.</p>"},{"location":"tutorials/query-results/#step-6-point-in-time-queries-with-as_of","title":"Step 6: Point-in-time queries with as_of","text":"<p>The <code>as_of</code> parameter narrows status lookups to a point in time:</p> <pre><code>from datetime import datetime, timezone\n\ndf = client.get_results(\n    family=\"uplift\",\n    status=\"production\",\n    metrics=[\"treatment_effect\"],\n    as_of=datetime(2026, 8, 15, tzinfo=timezone.utc),\n)\n</code></pre> <p>This returns whichever version was in production on 2026-08-15. You can also pass an ISO string:</p> <pre><code>df = client.get_results(\n    family=\"uplift\",\n    status=\"production\",\n    metrics=[\"treatment_effect\"],\n    as_of=\"2026-08-15T00:00:00+00:00\",\n)\n</code></pre> <p>Tip</p> <p>Point-in-time queries are useful for backfills: if you need to regenerate a downstream table for a past date, use <code>as_of</code> to get the version that was in production at that time.</p>"},{"location":"tutorials/query-results/#step-7-alias-resolution","title":"Step 7: Alias resolution","text":"<p>Consumers can request metrics by either their canonical name or any alias. The returned column always uses the canonical name:</p> <pre><code>df = client.get_results(\n    family=\"uplift\",\n    version=\"3.1.0\",\n    metrics=[\"te\"],  # alias for \"treatment_effect\"\n)\nprint(df.columns)\n# ['experiment_id', 'treatment_effect']\n</code></pre> <p>Requesting <code>\"te\"</code> returns a column named <code>\"treatment_effect\"</code> \u2014 the canonical name. This ensures that downstream code can rely on stable column names even as aliases are added across versions.</p> <p>You have now completed the full tutorial sequence. For task-oriented recipes, see the How-To Guides. For detailed API documentation, see the Reference.</p>"},{"location":"tutorials/register-and-validate/","title":"Register and Validate","text":"<p>In this tutorial you will validate a model spec against its physical tables and then register it in the causalops registry.</p> <p>Audience: Model producer.</p> <p>Prerequisites: Define a ModelSpec complete, with a <code>model_spec.py</code> defining an uplift spec.</p>"},{"location":"tutorials/register-and-validate/#step-1-seed-mock-data","title":"Step 1: Seed mock data","text":"<p>Before validating, the Parquet files referenced by the spec need to exist. The seed script generates mock data for the example models:</p> <pre><code>python scripts/seed_examples.py --example uplift-model\n</code></pre> <p>This creates Parquet files under <code>.causalops/data/uplift/</code>.</p>"},{"location":"tutorials/register-and-validate/#step-2-validate-the-spec","title":"Step 2: Validate the spec","text":"<p>The <code>validate</code> command checks that every table and column declared in the spec actually exists with the expected dtype \u2014 without registering anything:</p> <pre><code>causalops validate --spec-path model_spec.py\n</code></pre> <p>On success:</p> <pre><code>ok: uplift@3.1.0\n</code></pre>"},{"location":"tutorials/register-and-validate/#understanding-validation-outcomes","title":"Understanding validation outcomes","text":"<p>Validation distinguishes two failure modes:</p> Condition Severity Exit code Table does not exist yet Warning 0 Column missing in table Error Non-zero Column dtype mismatch Error Non-zero <p>Note</p> <p>A missing table produces a warning, not an error. This is intentional \u2014 on first registration the pipeline may not have run yet, so the table does not exist. Missing columns and dtype mismatches are hard errors because they indicate real drift between the spec and the data.</p> <p>If you have a dtype mismatch, the output looks like:</p> <pre><code>Errors:\n  .causalops/data/uplift/shared_v3.1.0.parquet.ate: spec says double, table has string\n</code></pre>"},{"location":"tutorials/register-and-validate/#step-3-register-the-spec","title":"Step 3: Register the spec","text":"<p>The <code>register</code> command validates the spec and then persists a registration record with its initial <code>experiment</code> status:</p> <pre><code>causalops register \\\n    --spec-path model_spec.py \\\n    --git-repo local/uplift-model \\\n    --git-tag v3.1.0 \\\n    --git-sha $(git rev-parse HEAD) \\\n    --registered-by \"$USER\"\n</code></pre> <p>On success:</p> <pre><code>Registered: uplift@3.1.0 (sha=a1b2c3d4)\n</code></pre>"},{"location":"tutorials/register-and-validate/#git-tag-must-match-spec-version","title":"Git tag must match spec version","text":"<p>The <code>--git-tag</code> value must be <code>v</code> followed by the spec's <code>version</code> field. If the spec says <code>version=\"3.1.0\"</code>, the tag must be <code>v3.1.0</code>. A mismatch fails registration:</p> <pre><code>Error: Git tag 'v3.0.0' does not match spec version (expected 'v3.1.0')\n</code></pre>"},{"location":"tutorials/register-and-validate/#environment-variable-fallbacks","title":"Environment variable fallbacks","text":"<p>In CI, you can skip the flags entirely \u2014 the CLI reads GitHub Actions environment variables by default:</p> Flag Environment variable <code>--git-repo</code> <code>GITHUB_REPOSITORY</code> <code>--git-tag</code> <code>GITHUB_REF_NAME</code> <code>--git-sha</code> <code>GITHUB_SHA</code> <code>--registered-by</code> <code>GITHUB_ACTOR</code> <p>So in a GitHub Actions workflow, <code>causalops register --spec-path model_spec.py</code> is sufficient \u2014 all four values come from the environment.</p>"},{"location":"tutorials/register-and-validate/#step-4-re-registration-with-force","title":"Step 4: Re-registration with --force","text":"<p>Attempting to register the same (family, version) pair twice fails:</p> <pre><code>Error: uplift@3.1.0 already registered. Use --force to overwrite.\n</code></pre> <p>Use <code>--force</code> to overwrite:</p> <pre><code>causalops register \\\n    --spec-path model_spec.py \\\n    --git-repo local/uplift-model \\\n    --git-tag v3.1.0 \\\n    --git-sha $(git rev-parse HEAD) \\\n    --registered-by \"$USER\" \\\n    --force\n</code></pre> <p>Warning</p> <p><code>--force</code> deletes the existing registration row but preserves the status history. Use it only when correcting a spec error \u2014 the audit trail of status events is not lost.</p>"},{"location":"tutorials/register-and-validate/#step-5-inspect-the-registry","title":"Step 5: Inspect the registry","text":"<p>The default JSON-file backend stores everything in <code>.causalops/registry.json</code>. Open it to see the registration and its initial status event:</p> <pre><code>python -m json.tool .causalops/registry.json\n</code></pre> <p>You will see two arrays:</p> <ul> <li><code>registrations</code> \u2014 one entry per (family, version) with the serialized   spec, git metadata, and registration timestamp.</li> <li><code>status_log</code> \u2014 an append-only list of status events. The initial   registration creates an <code>experiment</code> event automatically.</li> </ul> <pre><code>{\n  \"registrations\": [\n    {\n      \"family\": \"uplift\",\n      \"version\": \"3.1.0\",\n      \"spec_json\": \"...\",\n      \"git_repo\": \"local/uplift-model\",\n      \"git_tag\": \"v3.1.0\",\n      \"git_sha\": \"a1b2c3d4...\",\n      \"sdk_version\": \"0.1.0\",\n      \"registered_by\": \"alice\",\n      \"registered_at\": \"2026-09-06T12:00:00+00:00\"\n    }\n  ],\n  \"status_log\": [\n    {\n      \"event_id\": \"...\",\n      \"family\": \"uplift\",\n      \"version\": \"3.1.0\",\n      \"status\": \"experiment\",\n      \"effective_from\": \"2026-09-06T12:00:00+00:00\",\n      \"assigned_by\": \"alice\",\n      \"note\": \"\"\n    }\n  ]\n}\n</code></pre> <p>Next: Promote through the Lifecycle \u2014 move the registered version from experiment through challenger to production.</p>"}]}