# Utilities

Spark session factory and table reader. `build_local_spark_session` creates a minimal local session for development; `read_table` dispatches between filesystem paths and catalog identifiers.

::: causalops.utils.build_local_spark_session

::: causalops.utils.read_table
