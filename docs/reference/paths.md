# Paths

Default filesystem locations for local dev state. All paths are anchored under `<repo>/.causalops/` so each checkout is self-contained.

::: causalops.paths.find_repo_root

::: causalops.paths.default_data_dir

::: causalops.paths.default_registry_json_path
